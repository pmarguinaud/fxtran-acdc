MODULE UTIL_CPG_TNDHD_DDH_TYPE_MOD

USE CPG_DDH_TND_TYPE_MOD, ONLY : CPG_TNDHD_DDH_TYPE



INTERFACE ACDC_COPY
MODULE PROCEDURE ACDC_COPY_CPG_TNDHD_DDH_TYPE
END INTERFACE

INTERFACE ACDC_HOST
MODULE PROCEDURE ACDC_HOST_CPG_TNDHD_DDH_TYPE
END INTERFACE



INTERFACE ACDC_WIPE
MODULE PROCEDURE ACDC_WIPE_CPG_TNDHD_DDH_TYPE
END INTERFACE



CONTAINS





SUBROUTINE ACDC_COPY_CPG_TNDHD_DDH_TYPE (SELF, LDCREATED, LDFIELDAPI)
USE FIELD_UTIL_MODULE
IMPLICIT NONE
TYPE (CPG_TNDHD_DDH_TYPE), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDCREATED, LDFIELDAPI
LOGICAL :: LLCREATED
LOGICAL :: LLFIELDAPI
LOGICAL :: LF_NHX, LF_PD, LF_Q, LF_T, LF_U, LF_V, LF_VD, LZDATA_GFL, LZDATA_GMV, LZVIEW_GFL
LOGICAL :: LZVIEW_GMV
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LLCREATED = .FALSE.
IF (PRESENT (LDCREATED)) THEN
  LLCREATED = LDCREATED
ENDIF
IF (.NOT. LLCREATED) THEN
  !$OMP TARGET ENTER DATA MAP (ALLOC: SELF)
  !$OMP TARGET UPDATE TO (SELF)
ENDIF
LZDATA_GMV = ASSOCIATED (SELF%ZDATA_GMV)
IF (LZDATA_GMV) THEN
  !$OMP TARGET ENTER DATA MAP (ALLOC: SELF%ZDATA_GMV)
  !$OMP TARGET UPDATE TO (SELF%ZDATA_GMV)
  !$OMP TARGET ENTER DATA MAP (TO: SELF%ZDATA_GMV)
ENDIF

LZVIEW_GMV = ASSOCIATED (SELF%ZVIEW_GMV)
IF (LZVIEW_GMV) THEN
  !$OMP TARGET ENTER DATA MAP (ALLOC: SELF%ZVIEW_GMV)
  !$OMP TARGET UPDATE TO (SELF%ZVIEW_GMV)
  !$OMP TARGET ENTER DATA MAP (TO: SELF%ZVIEW_GMV)
ENDIF

LZDATA_GFL = ASSOCIATED (SELF%ZDATA_GFL)
IF (LZDATA_GFL) THEN
  !$OMP TARGET ENTER DATA MAP (ALLOC: SELF%ZDATA_GFL)
  !$OMP TARGET UPDATE TO (SELF%ZDATA_GFL)
  !$OMP TARGET ENTER DATA MAP (TO: SELF%ZDATA_GFL)
ENDIF

LZVIEW_GFL = ASSOCIATED (SELF%ZVIEW_GFL)
IF (LZVIEW_GFL) THEN
  !$OMP TARGET ENTER DATA MAP (ALLOC: SELF%ZVIEW_GFL)
  !$OMP TARGET UPDATE TO (SELF%ZVIEW_GFL)
  !$OMP TARGET ENTER DATA MAP (TO: SELF%ZVIEW_GFL)
ENDIF

LF_U = ASSOCIATED (SELF%F_U)
IF (LF_U .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_3RB (SELF%F_U)
  !$OMP TARGET ENTER DATA MAP (TO: SELF%F_U)
ENDIF

LF_V = ASSOCIATED (SELF%F_V)
IF (LF_V .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_3RB (SELF%F_V)
  !$OMP TARGET ENTER DATA MAP (TO: SELF%F_V)
ENDIF

LF_T = ASSOCIATED (SELF%F_T)
IF (LF_T .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_3RB (SELF%F_T)
  !$OMP TARGET ENTER DATA MAP (TO: SELF%F_T)
ENDIF

LF_PD = ASSOCIATED (SELF%F_PD)
IF (LF_PD .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_3RB (SELF%F_PD)
  !$OMP TARGET ENTER DATA MAP (TO: SELF%F_PD)
ENDIF

LF_VD = ASSOCIATED (SELF%F_VD)
IF (LF_VD .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_3RB (SELF%F_VD)
  !$OMP TARGET ENTER DATA MAP (TO: SELF%F_VD)
ENDIF

LF_NHX = ASSOCIATED (SELF%F_NHX)
IF (LF_NHX .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_3RB (SELF%F_NHX)
  !$OMP TARGET ENTER DATA MAP (TO: SELF%F_NHX)
ENDIF

LF_Q = ASSOCIATED (SELF%F_Q)
IF (LF_Q .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_3RB (SELF%F_Q)
  !$OMP TARGET ENTER DATA MAP (TO: SELF%F_Q)
ENDIF

END SUBROUTINE

SUBROUTINE ACDC_HOST_CPG_TNDHD_DDH_TYPE (SELF)
USE FIELD_UTIL_MODULE
IMPLICIT NONE
TYPE (CPG_TNDHD_DDH_TYPE), TARGET :: SELF
LOGICAL :: LF_NHX, LF_PD, LF_Q, LF_T, LF_U, LF_V, LF_VD, LZDATA_GFL, LZDATA_GMV, LZVIEW_GFL
LOGICAL :: LZVIEW_GMV

LF_U = ASSOCIATED (SELF%F_U)
IF (LF_U) THEN
  CALL HOST_FIELD_3RB (SELF%F_U)
ENDIF

LF_V = ASSOCIATED (SELF%F_V)
IF (LF_V) THEN
  CALL HOST_FIELD_3RB (SELF%F_V)
ENDIF

LF_T = ASSOCIATED (SELF%F_T)
IF (LF_T) THEN
  CALL HOST_FIELD_3RB (SELF%F_T)
ENDIF

LF_PD = ASSOCIATED (SELF%F_PD)
IF (LF_PD) THEN
  CALL HOST_FIELD_3RB (SELF%F_PD)
ENDIF

LF_VD = ASSOCIATED (SELF%F_VD)
IF (LF_VD) THEN
  CALL HOST_FIELD_3RB (SELF%F_VD)
ENDIF

LF_NHX = ASSOCIATED (SELF%F_NHX)
IF (LF_NHX) THEN
  CALL HOST_FIELD_3RB (SELF%F_NHX)
ENDIF

LF_Q = ASSOCIATED (SELF%F_Q)
IF (LF_Q) THEN
  CALL HOST_FIELD_3RB (SELF%F_Q)
ENDIF

END SUBROUTINE





SUBROUTINE ACDC_WIPE_CPG_TNDHD_DDH_TYPE (SELF, LDDELETED, LDFIELDAPI)
USE FIELD_UTIL_MODULE
IMPLICIT NONE
TYPE (CPG_TNDHD_DDH_TYPE), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LF_NHX, LF_PD, LF_Q, LF_T, LF_U, LF_V, LF_VD, LZDATA_GFL, LZDATA_GMV, LZVIEW_GFL
LOGICAL :: LZVIEW_GMV
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LZDATA_GMV = ASSOCIATED (SELF%ZDATA_GMV)
IF (LZDATA_GMV) THEN
  !$OMP TARGET EXIT DATA MAP (RELEASE: SELF%ZDATA_GMV)
  !$OMP TARGET EXIT DATA MAP (DELETE: SELF%ZDATA_GMV)
ENDIF

LZVIEW_GMV = ASSOCIATED (SELF%ZVIEW_GMV)
IF (LZVIEW_GMV) THEN
  !$OMP TARGET EXIT DATA MAP (RELEASE: SELF%ZVIEW_GMV)
  !$OMP TARGET EXIT DATA MAP (DELETE: SELF%ZVIEW_GMV)
ENDIF

LZDATA_GFL = ASSOCIATED (SELF%ZDATA_GFL)
IF (LZDATA_GFL) THEN
  !$OMP TARGET EXIT DATA MAP (RELEASE: SELF%ZDATA_GFL)
  !$OMP TARGET EXIT DATA MAP (DELETE: SELF%ZDATA_GFL)
ENDIF

LZVIEW_GFL = ASSOCIATED (SELF%ZVIEW_GFL)
IF (LZVIEW_GFL) THEN
  !$OMP TARGET EXIT DATA MAP (RELEASE: SELF%ZVIEW_GFL)
  !$OMP TARGET EXIT DATA MAP (DELETE: SELF%ZVIEW_GFL)
ENDIF

LF_U = ASSOCIATED (SELF%F_U)
IF (LF_U .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_U)
ENDIF

LF_V = ASSOCIATED (SELF%F_V)
IF (LF_V .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_V)
ENDIF

LF_T = ASSOCIATED (SELF%F_T)
IF (LF_T .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_T)
ENDIF

LF_PD = ASSOCIATED (SELF%F_PD)
IF (LF_PD .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_PD)
ENDIF

LF_VD = ASSOCIATED (SELF%F_VD)
IF (LF_VD .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_VD)
ENDIF

LF_NHX = ASSOCIATED (SELF%F_NHX)
IF (LF_NHX .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_NHX)
ENDIF

LF_Q = ASSOCIATED (SELF%F_Q)
IF (LF_Q .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_Q)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$OMP TARGET EXIT DATA MAP (DELETE: SELF)
ENDIF
END SUBROUTINE





END MODULE
