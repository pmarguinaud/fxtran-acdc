SUBMODULE (VARIABLE_MODULE) VARIABLE_MODULE_VARIABLE_2RB_LEGACY_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_LEGACY_VARIABLE_2RB (SELF, KADDRL, KADDRU, KDIR)
USE FIELD_UTIL_MODULE
IMPLICIT NONE
CLASS (VARIABLE_2RB), TARGET :: SELF
INTEGER*8, INTENT (IN) :: KADDRL
INTEGER*8, INTENT (IN) :: KADDRU
INTEGER, INTENT (IN) :: KDIR
CLASS (VARIABLE_BASE), POINTER :: YLSUPER
LOGICAL :: LFDL, LFDL9, LFDM, LFDM9, LFPC_PH, LFPH9, LFT0, LFT1, LFT9
YLSUPER => SELF
CALL YLSUPER%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
LFT0 = ASSOCIATED (SELF%FT0)
IF (LFT0) THEN
  CALL LEGACY_FIELD_2RB (SELF%FT0, KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LFT1 = ASSOCIATED (SELF%FT1)
IF (LFT1) THEN
  CALL LEGACY_FIELD_2RB (SELF%FT1, KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LFT9 = ASSOCIATED (SELF%FT9)
IF (LFT9) THEN
  CALL LEGACY_FIELD_2RB (SELF%FT9, KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LFPH9 = ASSOCIATED (SELF%FPH9)
IF (LFPH9) THEN
  CALL LEGACY_FIELD_2RB (SELF%FPH9, KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LFDL = ASSOCIATED (SELF%FDL)
IF (LFDL) THEN
  CALL LEGACY_FIELD_2RB (SELF%FDL, KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LFDM = ASSOCIATED (SELF%FDM)
IF (LFDM) THEN
  CALL LEGACY_FIELD_2RB (SELF%FDM, KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LFDL9 = ASSOCIATED (SELF%FDL9)
IF (LFDL9) THEN
  CALL LEGACY_FIELD_2RB (SELF%FDL9, KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LFDM9 = ASSOCIATED (SELF%FDM9)
IF (LFDM9) THEN
  CALL LEGACY_FIELD_2RB (SELF%FDM9, KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LFPC_PH = ASSOCIATED (SELF%FPC_PH)
IF (LFPC_PH) THEN
  CALL LEGACY_FIELD_2RB (SELF%FPC_PH, KADDRL, KADDRU, KDIR=KDIR)
ENDIF

END SUBROUTINE

END SUBMODULE
