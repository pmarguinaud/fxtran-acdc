SUBMODULE (YOMGPDDH) YOMGPDDH_TGPDDH_WIPE_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_WIPE_TGPDDH (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
CLASS (TGPDDH), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LGFLTNDHD_DDH, LGFLTNDSL_DDH, LGMVTNDHD_DDH, LGMVTNDSI_DDH, LGMVTNDSL_DDH
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LGMVTNDHD_DDH = ALLOCATED (SELF%GMVTNDHD_DDH)
IF (LGMVTNDHD_DDH) THEN
  !$ACC EXIT DATA DETACH (SELF%GMVTNDHD_DDH)
  !$ACC EXIT DATA DELETE (SELF%GMVTNDHD_DDH)
ENDIF

LGFLTNDHD_DDH = ALLOCATED (SELF%GFLTNDHD_DDH)
IF (LGFLTNDHD_DDH) THEN
  !$ACC EXIT DATA DETACH (SELF%GFLTNDHD_DDH)
  !$ACC EXIT DATA DELETE (SELF%GFLTNDHD_DDH)
ENDIF

LGMVTNDSI_DDH = ALLOCATED (SELF%GMVTNDSI_DDH)
IF (LGMVTNDSI_DDH) THEN
  !$ACC EXIT DATA DETACH (SELF%GMVTNDSI_DDH)
  !$ACC EXIT DATA DELETE (SELF%GMVTNDSI_DDH)
ENDIF

LGMVTNDSL_DDH = ALLOCATED (SELF%GMVTNDSL_DDH)
IF (LGMVTNDSL_DDH) THEN
  !$ACC EXIT DATA DETACH (SELF%GMVTNDSL_DDH)
  !$ACC EXIT DATA DELETE (SELF%GMVTNDSL_DDH)
ENDIF

LGFLTNDSL_DDH = ALLOCATED (SELF%GFLTNDSL_DDH)
IF (LGFLTNDSL_DDH) THEN
  !$ACC EXIT DATA DETACH (SELF%GFLTNDSL_DDH)
  !$ACC EXIT DATA DELETE (SELF%GFLTNDSL_DDH)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

END SUBMODULE
