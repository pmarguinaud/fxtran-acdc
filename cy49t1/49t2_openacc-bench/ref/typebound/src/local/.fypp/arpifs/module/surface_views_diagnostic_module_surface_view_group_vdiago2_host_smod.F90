SUBMODULE (SURFACE_VIEWS_DIAGNOSTIC_MODULE) SURFACE_VIEWS_DIAGNOSTIC_MODULE_SURFACE_VIEW_GROUP_VDIAGO2_HOST_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE HOST_SURFACE_VIEW_GROUP_VDIAGO2 (SELF)
USE FIELD_UTIL_MODULE

IMPLICIT NONE
CLASS (SURFACE_VIEW_GROUP_VDIAGO2), TARGET :: SELF
LOGICAL :: LF_GROUP, LF_OCDEP, LF_USTRC, LF_VSTRC, LVARIABLE_GROUP
LVARIABLE_GROUP = ASSOCIATED (SELF%VARIABLE_GROUP)
IF (LVARIABLE_GROUP) THEN
  CALL SELF%VARIABLE_GROUP%HOST ()
ENDIF

LF_OCDEP = ASSOCIATED (SELF%F_OCDEP)
IF (LF_OCDEP) THEN
  CALL HOST_FIELD_2RB (SELF%F_OCDEP)
ENDIF

LF_USTRC = ASSOCIATED (SELF%F_USTRC)
IF (LF_USTRC) THEN
  CALL HOST_FIELD_2RB (SELF%F_USTRC)
ENDIF

LF_VSTRC = ASSOCIATED (SELF%F_VSTRC)
IF (LF_VSTRC) THEN
  CALL HOST_FIELD_2RB (SELF%F_VSTRC)
ENDIF

LF_GROUP = ASSOCIATED (SELF%F_GROUP)
IF (LF_GROUP) THEN
  CALL HOST_FIELD_3RB (SELF%F_GROUP)
ENDIF

END SUBROUTINE

END SUBMODULE
