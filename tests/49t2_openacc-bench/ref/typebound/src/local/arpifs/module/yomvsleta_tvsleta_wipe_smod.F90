SUBMODULE (YOMVSLETA) YOMVSLETA_TVSLETA_WIPE_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_WIPE_TVSLETA (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
CLASS (TVSLETA), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LGAMMA_WENO, LNVAUTF, LNVAUTH, LVCUICO, LVCUICOH, LVRDETAR, LVSLD, LVSLDH, LVSLDW, LVSLDWH
LOGICAL :: LVSLVF, LVSLVFH
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LVCUICO = ALLOCATED (SELF%VCUICO)
IF (LVCUICO) THEN
  !$ACC EXIT DATA DETACH (SELF%VCUICO)
  !$ACC EXIT DATA DELETE (SELF%VCUICO)
ENDIF

LVCUICOH = ALLOCATED (SELF%VCUICOH)
IF (LVCUICOH) THEN
  !$ACC EXIT DATA DETACH (SELF%VCUICOH)
  !$ACC EXIT DATA DELETE (SELF%VCUICOH)
ENDIF

LVSLD = ALLOCATED (SELF%VSLD)
IF (LVSLD) THEN
  !$ACC EXIT DATA DETACH (SELF%VSLD)
  !$ACC EXIT DATA DELETE (SELF%VSLD)
ENDIF

LVSLDH = ALLOCATED (SELF%VSLDH)
IF (LVSLDH) THEN
  !$ACC EXIT DATA DETACH (SELF%VSLDH)
  !$ACC EXIT DATA DELETE (SELF%VSLDH)
ENDIF

LVSLDW = ALLOCATED (SELF%VSLDW)
IF (LVSLDW) THEN
  !$ACC EXIT DATA DETACH (SELF%VSLDW)
  !$ACC EXIT DATA DELETE (SELF%VSLDW)
ENDIF

LVSLDWH = ALLOCATED (SELF%VSLDWH)
IF (LVSLDWH) THEN
  !$ACC EXIT DATA DETACH (SELF%VSLDWH)
  !$ACC EXIT DATA DELETE (SELF%VSLDWH)
ENDIF

LVSLVF = ALLOCATED (SELF%VSLVF)
IF (LVSLVF) THEN
  !$ACC EXIT DATA DETACH (SELF%VSLVF)
  !$ACC EXIT DATA DELETE (SELF%VSLVF)
ENDIF

LVSLVFH = ALLOCATED (SELF%VSLVFH)
IF (LVSLVFH) THEN
  !$ACC EXIT DATA DETACH (SELF%VSLVFH)
  !$ACC EXIT DATA DELETE (SELF%VSLVFH)
ENDIF

LGAMMA_WENO = ALLOCATED (SELF%GAMMA_WENO)
IF (LGAMMA_WENO) THEN
  !$ACC EXIT DATA DETACH (SELF%GAMMA_WENO)
  !$ACC EXIT DATA DELETE (SELF%GAMMA_WENO)
ENDIF

LVRDETAR = ALLOCATED (SELF%VRDETAR)
IF (LVRDETAR) THEN
  !$ACC EXIT DATA DETACH (SELF%VRDETAR)
  !$ACC EXIT DATA DELETE (SELF%VRDETAR)
ENDIF

LNVAUTF = ALLOCATED (SELF%NVAUTF)
IF (LNVAUTF) THEN
  !$ACC EXIT DATA DETACH (SELF%NVAUTF)
  !$ACC EXIT DATA DELETE (SELF%NVAUTF)
ENDIF

LNVAUTH = ALLOCATED (SELF%NVAUTH)
IF (LNVAUTH) THEN
  !$ACC EXIT DATA DETACH (SELF%NVAUTH)
  !$ACC EXIT DATA DELETE (SELF%NVAUTH)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

END SUBMODULE
