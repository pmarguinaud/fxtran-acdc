SUBMODULE (MODD_PARAM_LIMA_WARM) MODD_PARAM_LIMA_WARM_PARAM_LIMA_WARM_T_WIPE_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_WIPE_PARAM_LIMA_WARM_T (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
CLASS (PARAM_LIMA_WARM_T), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LXAHENF, LXAHENG, LXAHENG2, LXAHENG3, LXAHENY, LXCONCC_INI, LXHYPF12, LXHYPF32, LXPSI1, LXPSI3
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF

LXHYPF12 = ALLOCATED (SELF%XHYPF12)
IF (LXHYPF12) THEN
  !$ACC EXIT DATA DETACH (SELF%XHYPF12)
  !$ACC EXIT DATA DELETE (SELF%XHYPF12)
ENDIF

LXHYPF32 = ALLOCATED (SELF%XHYPF32)
IF (LXHYPF32) THEN
  !$ACC EXIT DATA DETACH (SELF%XHYPF32)
  !$ACC EXIT DATA DELETE (SELF%XHYPF32)
ENDIF

LXAHENG = ALLOCATED (SELF%XAHENG)
IF (LXAHENG) THEN
  !$ACC EXIT DATA DETACH (SELF%XAHENG)
  !$ACC EXIT DATA DELETE (SELF%XAHENG)
ENDIF

LXAHENG2 = ALLOCATED (SELF%XAHENG2)
IF (LXAHENG2) THEN
  !$ACC EXIT DATA DETACH (SELF%XAHENG2)
  !$ACC EXIT DATA DELETE (SELF%XAHENG2)
ENDIF

LXAHENG3 = ALLOCATED (SELF%XAHENG3)
IF (LXAHENG3) THEN
  !$ACC EXIT DATA DETACH (SELF%XAHENG3)
  !$ACC EXIT DATA DELETE (SELF%XAHENG3)
ENDIF

LXPSI1 = ALLOCATED (SELF%XPSI1)
IF (LXPSI1) THEN
  !$ACC EXIT DATA DETACH (SELF%XPSI1)
  !$ACC EXIT DATA DELETE (SELF%XPSI1)
ENDIF

LXPSI3 = ALLOCATED (SELF%XPSI3)
IF (LXPSI3) THEN
  !$ACC EXIT DATA DETACH (SELF%XPSI3)
  !$ACC EXIT DATA DELETE (SELF%XPSI3)
ENDIF

LXAHENF = ALLOCATED (SELF%XAHENF)
IF (LXAHENF) THEN
  !$ACC EXIT DATA DETACH (SELF%XAHENF)
  !$ACC EXIT DATA DELETE (SELF%XAHENF)
ENDIF

LXAHENY = ALLOCATED (SELF%XAHENY)
IF (LXAHENY) THEN
  !$ACC EXIT DATA DETACH (SELF%XAHENY)
  !$ACC EXIT DATA DELETE (SELF%XAHENY)
ENDIF

LXCONCC_INI = ALLOCATED (SELF%XCONCC_INI)
IF (LXCONCC_INI) THEN
  !$ACC EXIT DATA DETACH (SELF%XCONCC_INI)
  !$ACC EXIT DATA DELETE (SELF%XCONCC_INI)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

END SUBMODULE
