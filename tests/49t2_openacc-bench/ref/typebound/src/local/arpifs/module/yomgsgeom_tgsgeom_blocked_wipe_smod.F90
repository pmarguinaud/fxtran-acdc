SUBMODULE (YOMGSGEOM) YOMGSGEOM_TGSGEOM_BLOCKED_WIPE_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_WIPE_TGSGEOM_BLOCKED (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
CLASS (TGSGEOM_BLOCKED), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LGAW, LGECLO, LGELAM, LGELAMDEG, LGELAMINF, LGELAMSUP, LGELAT, LGEMU, LGESLO, LGEXCO
LOGICAL :: LGEYCO, LGEZCO, LGM, LGMAPPA, LGNORDL, LGNORDLCL, LGNORDM, LGNORDMCL, LGNORDMCM, LGOMVRL
LOGICAL :: LGOMVRM, LGSQM2, LNGPLAT, LNUNIQUEGP, LRCORI, LRCORIC
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LRCORI = ASSOCIATED (SELF%RCORI)
IF (LRCORI) THEN
  !$ACC EXIT DATA DETACH (SELF%RCORI)
  !$ACC EXIT DATA DELETE (SELF%RCORI)
ENDIF

LRCORIC = ASSOCIATED (SELF%RCORIC)
IF (LRCORIC) THEN
  !$ACC EXIT DATA DETACH (SELF%RCORIC)
  !$ACC EXIT DATA DELETE (SELF%RCORIC)
ENDIF

LGEMU = ASSOCIATED (SELF%GEMU)
IF (LGEMU) THEN
  !$ACC EXIT DATA DETACH (SELF%GEMU)
  !$ACC EXIT DATA DELETE (SELF%GEMU)
ENDIF

LGSQM2 = ASSOCIATED (SELF%GSQM2)
IF (LGSQM2) THEN
  !$ACC EXIT DATA DETACH (SELF%GSQM2)
  !$ACC EXIT DATA DELETE (SELF%GSQM2)
ENDIF

LGELAM = ASSOCIATED (SELF%GELAM)
IF (LGELAM) THEN
  !$ACC EXIT DATA DETACH (SELF%GELAM)
  !$ACC EXIT DATA DELETE (SELF%GELAM)
ENDIF

LGELAT = ASSOCIATED (SELF%GELAT)
IF (LGELAT) THEN
  !$ACC EXIT DATA DETACH (SELF%GELAT)
  !$ACC EXIT DATA DELETE (SELF%GELAT)
ENDIF

LGECLO = ASSOCIATED (SELF%GECLO)
IF (LGECLO) THEN
  !$ACC EXIT DATA DETACH (SELF%GECLO)
  !$ACC EXIT DATA DELETE (SELF%GECLO)
ENDIF

LGESLO = ASSOCIATED (SELF%GESLO)
IF (LGESLO) THEN
  !$ACC EXIT DATA DETACH (SELF%GESLO)
  !$ACC EXIT DATA DELETE (SELF%GESLO)
ENDIF

LGELAMDEG = ASSOCIATED (SELF%GELAMDEG)
IF (LGELAMDEG) THEN
  !$ACC EXIT DATA DETACH (SELF%GELAMDEG)
  !$ACC EXIT DATA DELETE (SELF%GELAMDEG)
ENDIF

LGELAMINF = ASSOCIATED (SELF%GELAMINF)
IF (LGELAMINF) THEN
  !$ACC EXIT DATA DETACH (SELF%GELAMINF)
  !$ACC EXIT DATA DELETE (SELF%GELAMINF)
ENDIF

LGELAMSUP = ASSOCIATED (SELF%GELAMSUP)
IF (LGELAMSUP) THEN
  !$ACC EXIT DATA DETACH (SELF%GELAMSUP)
  !$ACC EXIT DATA DELETE (SELF%GELAMSUP)
ENDIF

LGEXCO = ASSOCIATED (SELF%GEXCO)
IF (LGEXCO) THEN
  !$ACC EXIT DATA DETACH (SELF%GEXCO)
  !$ACC EXIT DATA DELETE (SELF%GEXCO)
ENDIF

LGEYCO = ASSOCIATED (SELF%GEYCO)
IF (LGEYCO) THEN
  !$ACC EXIT DATA DETACH (SELF%GEYCO)
  !$ACC EXIT DATA DELETE (SELF%GEYCO)
ENDIF

LGEZCO = ASSOCIATED (SELF%GEZCO)
IF (LGEZCO) THEN
  !$ACC EXIT DATA DETACH (SELF%GEZCO)
  !$ACC EXIT DATA DELETE (SELF%GEZCO)
ENDIF

LGM = ASSOCIATED (SELF%GM)
IF (LGM) THEN
  !$ACC EXIT DATA DETACH (SELF%GM)
  !$ACC EXIT DATA DELETE (SELF%GM)
ENDIF

LGMAPPA = ASSOCIATED (SELF%GMAPPA)
IF (LGMAPPA) THEN
  !$ACC EXIT DATA DETACH (SELF%GMAPPA)
  !$ACC EXIT DATA DELETE (SELF%GMAPPA)
ENDIF

LGOMVRL = ASSOCIATED (SELF%GOMVRL)
IF (LGOMVRL) THEN
  !$ACC EXIT DATA DETACH (SELF%GOMVRL)
  !$ACC EXIT DATA DELETE (SELF%GOMVRL)
ENDIF

LGOMVRM = ASSOCIATED (SELF%GOMVRM)
IF (LGOMVRM) THEN
  !$ACC EXIT DATA DETACH (SELF%GOMVRM)
  !$ACC EXIT DATA DELETE (SELF%GOMVRM)
ENDIF

LGNORDL = ASSOCIATED (SELF%GNORDL)
IF (LGNORDL) THEN
  !$ACC EXIT DATA DETACH (SELF%GNORDL)
  !$ACC EXIT DATA DELETE (SELF%GNORDL)
ENDIF

LGNORDM = ASSOCIATED (SELF%GNORDM)
IF (LGNORDM) THEN
  !$ACC EXIT DATA DETACH (SELF%GNORDM)
  !$ACC EXIT DATA DELETE (SELF%GNORDM)
ENDIF

LGNORDLCL = ASSOCIATED (SELF%GNORDLCL)
IF (LGNORDLCL) THEN
  !$ACC EXIT DATA DETACH (SELF%GNORDLCL)
  !$ACC EXIT DATA DELETE (SELF%GNORDLCL)
ENDIF

LGNORDMCL = ASSOCIATED (SELF%GNORDMCL)
IF (LGNORDMCL) THEN
  !$ACC EXIT DATA DETACH (SELF%GNORDMCL)
  !$ACC EXIT DATA DELETE (SELF%GNORDMCL)
ENDIF

LGNORDMCM = ASSOCIATED (SELF%GNORDMCM)
IF (LGNORDMCM) THEN
  !$ACC EXIT DATA DETACH (SELF%GNORDMCM)
  !$ACC EXIT DATA DELETE (SELF%GNORDMCM)
ENDIF

LGAW = ASSOCIATED (SELF%GAW)
IF (LGAW) THEN
  !$ACC EXIT DATA DETACH (SELF%GAW)
  !$ACC EXIT DATA DELETE (SELF%GAW)
ENDIF

LNGPLAT = ASSOCIATED (SELF%NGPLAT)
IF (LNGPLAT) THEN
  !$ACC EXIT DATA DETACH (SELF%NGPLAT)
  !$ACC EXIT DATA DELETE (SELF%NGPLAT)
ENDIF

LNUNIQUEGP = ASSOCIATED (SELF%NUNIQUEGP)
IF (LNUNIQUEGP) THEN
  !$ACC EXIT DATA DETACH (SELF%NUNIQUEGP)
  !$ACC EXIT DATA DELETE (SELF%NUNIQUEGP)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

END SUBMODULE
