MODULE UTIL_TYPE_SFL_VCLIH_MOD

USE SURFACE_FIELDS_MIX, ONLY : TYPE_SFL_VCLIH

INTERFACE SAVE
MODULE PROCEDURE SAVE_TYPE_SFL_VCLIH
END INTERFACE

INTERFACE LOAD
MODULE PROCEDURE LOAD_TYPE_SFL_VCLIH
END INTERFACE

INTERFACE COPY
MODULE PROCEDURE COPY_TYPE_SFL_VCLIH
END INTERFACE

INTERFACE HOST
MODULE PROCEDURE HOST_TYPE_SFL_VCLIH
END INTERFACE

     
INTERFACE CRC64
MODULE PROCEDURE CRC64_TYPE_SFL_VCLIH
END INTERFACE

INTERFACE WIPE
MODULE PROCEDURE WIPE_TYPE_SFL_VCLIH
END INTERFACE



CONTAINS

SUBROUTINE SAVE_TYPE_SFL_VCLIH (SELF, KLUN)
USE UTIL_TYPE_SURF_MTL_2D_MOD
IMPLICIT NONE
TYPE (TYPE_SFL_VCLIH), INTENT (IN), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN
INTEGER :: J1
LOGICAL :: LYBCCH, LYEVA, LYPBLH, LYPCL, LYPCN, LYPSL, LYPSN, LYQSH, LYSCCH, LYSPSH
LOGICAL :: LYTCCH, LYVH
LYTCCH = ASSOCIATED (SELF%YTCCH)
WRITE (KLUN) LYTCCH
IF (LYTCCH) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YTCCH, KLUN)
ENDIF
LYSCCH = ASSOCIATED (SELF%YSCCH)
WRITE (KLUN) LYSCCH
IF (LYSCCH) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YSCCH, KLUN)
ENDIF
LYBCCH = ASSOCIATED (SELF%YBCCH)
WRITE (KLUN) LYBCCH
IF (LYBCCH) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YBCCH, KLUN)
ENDIF
LYPBLH = ASSOCIATED (SELF%YPBLH)
WRITE (KLUN) LYPBLH
IF (LYPBLH) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YPBLH, KLUN)
ENDIF
LYSPSH = ASSOCIATED (SELF%YSPSH)
WRITE (KLUN) LYSPSH
IF (LYSPSH) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YSPSH, KLUN)
ENDIF
LYQSH = ASSOCIATED (SELF%YQSH)
WRITE (KLUN) LYQSH
IF (LYQSH) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YQSH, KLUN)
ENDIF
LYVH = ASSOCIATED (SELF%YVH)
WRITE (KLUN) LYVH
IF (LYVH) THEN
  WRITE (KLUN) LBOUND (SELF%YVH)
  WRITE (KLUN) UBOUND (SELF%YVH)
  DO J1 = LBOUND (SELF%YVH, 1), UBOUND (SELF%YVH, 1)
    CALL SAVE_TYPE_SURF_MTL_2D (SELF%YVH (J1), KLUN)
  ENDDO
ENDIF
LYPCL = ASSOCIATED (SELF%YPCL)
WRITE (KLUN) LYPCL
IF (LYPCL) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YPCL, KLUN)
ENDIF
LYPSL = ASSOCIATED (SELF%YPSL)
WRITE (KLUN) LYPSL
IF (LYPSL) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YPSL, KLUN)
ENDIF
LYPCN = ASSOCIATED (SELF%YPCN)
WRITE (KLUN) LYPCN
IF (LYPCN) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YPCN, KLUN)
ENDIF
LYPSN = ASSOCIATED (SELF%YPSN)
WRITE (KLUN) LYPSN
IF (LYPSN) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YPSN, KLUN)
ENDIF
LYEVA = ASSOCIATED (SELF%YEVA)
WRITE (KLUN) LYEVA
IF (LYEVA) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YEVA, KLUN)
ENDIF
END SUBROUTINE

SUBROUTINE LOAD_TYPE_SFL_VCLIH (SELF, KLUN)
USE UTIL_TYPE_SURF_MTL_2D_MOD

IMPLICIT NONE
TYPE (TYPE_SFL_VCLIH), INTENT (OUT), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN
INTEGER :: J1
INTEGER :: IL1(1), IU1(1)
LOGICAL :: LYBCCH, LYEVA, LYPBLH, LYPCL, LYPCN, LYPSL, LYPSN, LYQSH, LYSCCH, LYSPSH
LOGICAL :: LYTCCH, LYVH
READ (KLUN) LYTCCH
IF (LYTCCH) THEN
  ALLOCATE (SELF%YTCCH)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YTCCH, KLUN)
ELSE
  NULLIFY (SELF%YTCCH)
ENDIF
READ (KLUN) LYSCCH
IF (LYSCCH) THEN
  ALLOCATE (SELF%YSCCH)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YSCCH, KLUN)
ELSE
  NULLIFY (SELF%YSCCH)
ENDIF
READ (KLUN) LYBCCH
IF (LYBCCH) THEN
  ALLOCATE (SELF%YBCCH)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YBCCH, KLUN)
ELSE
  NULLIFY (SELF%YBCCH)
ENDIF
READ (KLUN) LYPBLH
IF (LYPBLH) THEN
  ALLOCATE (SELF%YPBLH)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YPBLH, KLUN)
ELSE
  NULLIFY (SELF%YPBLH)
ENDIF
READ (KLUN) LYSPSH
IF (LYSPSH) THEN
  ALLOCATE (SELF%YSPSH)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YSPSH, KLUN)
ELSE
  NULLIFY (SELF%YSPSH)
ENDIF
READ (KLUN) LYQSH
IF (LYQSH) THEN
  ALLOCATE (SELF%YQSH)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YQSH, KLUN)
ELSE
  NULLIFY (SELF%YQSH)
ENDIF
READ (KLUN) LYVH
IF (LYVH) THEN
  READ (KLUN) IL1
  READ (KLUN) IU1
  ALLOCATE (SELF%YVH (IL1(1):IU1(1)))
  DO J1 = LBOUND (SELF%YVH, 1), UBOUND (SELF%YVH, 1)
    CALL LOAD_TYPE_SURF_MTL_2D (SELF%YVH (J1), KLUN)
  ENDDO
ELSE
  NULLIFY (SELF%YVH)
ENDIF
READ (KLUN) LYPCL
IF (LYPCL) THEN
  ALLOCATE (SELF%YPCL)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YPCL, KLUN)
ELSE
  NULLIFY (SELF%YPCL)
ENDIF
READ (KLUN) LYPSL
IF (LYPSL) THEN
  ALLOCATE (SELF%YPSL)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YPSL, KLUN)
ELSE
  NULLIFY (SELF%YPSL)
ENDIF
READ (KLUN) LYPCN
IF (LYPCN) THEN
  ALLOCATE (SELF%YPCN)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YPCN, KLUN)
ELSE
  NULLIFY (SELF%YPCN)
ENDIF
READ (KLUN) LYPSN
IF (LYPSN) THEN
  ALLOCATE (SELF%YPSN)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YPSN, KLUN)
ELSE
  NULLIFY (SELF%YPSN)
ENDIF
READ (KLUN) LYEVA
IF (LYEVA) THEN
  ALLOCATE (SELF%YEVA)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YEVA, KLUN)
ELSE
  NULLIFY (SELF%YEVA)
ENDIF
END SUBROUTINE


SUBROUTINE COPY_TYPE_SFL_VCLIH (SELF, LDCREATED, LDFIELDAPI)
USE UTIL_TYPE_SURF_MTL_2D_MOD
IMPLICIT NONE
TYPE (TYPE_SFL_VCLIH), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDCREATED, LDFIELDAPI
LOGICAL :: LLCREATED
LOGICAL :: LLFIELDAPI
INTEGER :: J1
LOGICAL :: LYBCCH, LYEVA, LYPBLH, LYPCL, LYPCN, LYPSL, LYPSN, LYQSH, LYSCCH, LYSPSH
LOGICAL :: LYTCCH, LYVH

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LLCREATED = .FALSE.
IF (PRESENT (LDCREATED)) THEN
  LLCREATED = LDCREATED
ENDIF
IF (.NOT. LLCREATED) THEN
  !$ACC ENTER DATA CREATE (SELF)
  !$ACC UPDATE DEVICE (SELF)
ENDIF
LYTCCH = ASSOCIATED (SELF%YTCCH)
IF (LYTCCH) THEN
  !$ACC ENTER DATA CREATE (SELF%YTCCH)
  !$ACC UPDATE DEVICE (SELF%YTCCH)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YTCCH, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YTCCH)
ENDIF

LYSCCH = ASSOCIATED (SELF%YSCCH)
IF (LYSCCH) THEN
  !$ACC ENTER DATA CREATE (SELF%YSCCH)
  !$ACC UPDATE DEVICE (SELF%YSCCH)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YSCCH, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YSCCH)
ENDIF

LYBCCH = ASSOCIATED (SELF%YBCCH)
IF (LYBCCH) THEN
  !$ACC ENTER DATA CREATE (SELF%YBCCH)
  !$ACC UPDATE DEVICE (SELF%YBCCH)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YBCCH, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YBCCH)
ENDIF

LYPBLH = ASSOCIATED (SELF%YPBLH)
IF (LYPBLH) THEN
  !$ACC ENTER DATA CREATE (SELF%YPBLH)
  !$ACC UPDATE DEVICE (SELF%YPBLH)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YPBLH, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YPBLH)
ENDIF

LYSPSH = ASSOCIATED (SELF%YSPSH)
IF (LYSPSH) THEN
  !$ACC ENTER DATA CREATE (SELF%YSPSH)
  !$ACC UPDATE DEVICE (SELF%YSPSH)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YSPSH, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YSPSH)
ENDIF

LYQSH = ASSOCIATED (SELF%YQSH)
IF (LYQSH) THEN
  !$ACC ENTER DATA CREATE (SELF%YQSH)
  !$ACC UPDATE DEVICE (SELF%YQSH)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YQSH, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YQSH)
ENDIF

LYVH = ASSOCIATED (SELF%YVH)
IF (LYVH) THEN
  !$ACC ENTER DATA CREATE (SELF%YVH)
  !$ACC UPDATE DEVICE (SELF%YVH)
  DO J1 = LBOUND (SELF%YVH, 1), UBOUND (SELF%YVH, 1)
    CALL COPY_TYPE_SURF_MTL_2D (SELF%YVH (J1), LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  ENDDO
  !$ACC ENTER DATA ATTACH (SELF%YVH)
ENDIF

LYPCL = ASSOCIATED (SELF%YPCL)
IF (LYPCL) THEN
  !$ACC ENTER DATA CREATE (SELF%YPCL)
  !$ACC UPDATE DEVICE (SELF%YPCL)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YPCL, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YPCL)
ENDIF

LYPSL = ASSOCIATED (SELF%YPSL)
IF (LYPSL) THEN
  !$ACC ENTER DATA CREATE (SELF%YPSL)
  !$ACC UPDATE DEVICE (SELF%YPSL)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YPSL, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YPSL)
ENDIF

LYPCN = ASSOCIATED (SELF%YPCN)
IF (LYPCN) THEN
  !$ACC ENTER DATA CREATE (SELF%YPCN)
  !$ACC UPDATE DEVICE (SELF%YPCN)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YPCN, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YPCN)
ENDIF

LYPSN = ASSOCIATED (SELF%YPSN)
IF (LYPSN) THEN
  !$ACC ENTER DATA CREATE (SELF%YPSN)
  !$ACC UPDATE DEVICE (SELF%YPSN)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YPSN, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YPSN)
ENDIF

LYEVA = ASSOCIATED (SELF%YEVA)
IF (LYEVA) THEN
  !$ACC ENTER DATA CREATE (SELF%YEVA)
  !$ACC UPDATE DEVICE (SELF%YEVA)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YEVA, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YEVA)
ENDIF

END SUBROUTINE

SUBROUTINE HOST_TYPE_SFL_VCLIH (SELF)
USE UTIL_TYPE_SURF_MTL_2D_MOD

IMPLICIT NONE
TYPE (TYPE_SFL_VCLIH), TARGET :: SELF
INTEGER :: J1
LOGICAL :: LYBCCH, LYEVA, LYPBLH, LYPCL, LYPCN, LYPSL, LYPSN, LYQSH, LYSCCH, LYSPSH
LOGICAL :: LYTCCH, LYVH
LYTCCH = ASSOCIATED (SELF%YTCCH)
IF (LYTCCH) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YTCCH)
ENDIF

LYSCCH = ASSOCIATED (SELF%YSCCH)
IF (LYSCCH) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YSCCH)
ENDIF

LYBCCH = ASSOCIATED (SELF%YBCCH)
IF (LYBCCH) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YBCCH)
ENDIF

LYPBLH = ASSOCIATED (SELF%YPBLH)
IF (LYPBLH) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YPBLH)
ENDIF

LYSPSH = ASSOCIATED (SELF%YSPSH)
IF (LYSPSH) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YSPSH)
ENDIF

LYQSH = ASSOCIATED (SELF%YQSH)
IF (LYQSH) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YQSH)
ENDIF

LYVH = ASSOCIATED (SELF%YVH)
IF (LYVH) THEN
  DO J1 = LBOUND (SELF%YVH, 1), UBOUND (SELF%YVH, 1)
    CALL HOST_TYPE_SURF_MTL_2D (SELF%YVH (J1))
  ENDDO
ENDIF

LYPCL = ASSOCIATED (SELF%YPCL)
IF (LYPCL) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YPCL)
ENDIF

LYPSL = ASSOCIATED (SELF%YPSL)
IF (LYPSL) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YPSL)
ENDIF

LYPCN = ASSOCIATED (SELF%YPCN)
IF (LYPCN) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YPCN)
ENDIF

LYPSN = ASSOCIATED (SELF%YPSN)
IF (LYPSN) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YPSN)
ENDIF

LYEVA = ASSOCIATED (SELF%YEVA)
IF (LYEVA) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YEVA)
ENDIF

END SUBROUTINE

     

SUBROUTINE CRC64_TYPE_SFL_VCLIH (SELF, KLUN, CDPATH)
USE UTIL_TYPE_SURF_MTL_2D_MOD
USE CRC64_INTRINSIC, ONLY : FCRC64 => CRC64

IMPLICIT NONE
TYPE (TYPE_SFL_VCLIH), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN
CHARACTER(LEN=*), INTENT (IN) :: CDPATH
CHARACTER(LEN=128) :: CLIND
INTEGER :: J1
LOGICAL :: LYBCCH, LYEVA, LYPBLH, LYPCL, LYPCN, LYPSL, LYPSN, LYQSH, LYSCCH, LYSPSH
LOGICAL :: LYTCCH, LYVH
LYTCCH = ASSOCIATED (SELF%YTCCH)
IF (LYTCCH) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YTCCH, KLUN, CDPATH//'%YTCCH'//TRIM(CLIND))
ENDIF

LYSCCH = ASSOCIATED (SELF%YSCCH)
IF (LYSCCH) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YSCCH, KLUN, CDPATH//'%YSCCH'//TRIM(CLIND))
ENDIF

LYBCCH = ASSOCIATED (SELF%YBCCH)
IF (LYBCCH) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YBCCH, KLUN, CDPATH//'%YBCCH'//TRIM(CLIND))
ENDIF

LYPBLH = ASSOCIATED (SELF%YPBLH)
IF (LYPBLH) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YPBLH, KLUN, CDPATH//'%YPBLH'//TRIM(CLIND))
ENDIF

LYSPSH = ASSOCIATED (SELF%YSPSH)
IF (LYSPSH) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YSPSH, KLUN, CDPATH//'%YSPSH'//TRIM(CLIND))
ENDIF

LYQSH = ASSOCIATED (SELF%YQSH)
IF (LYQSH) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YQSH, KLUN, CDPATH//'%YQSH'//TRIM(CLIND))
ENDIF

LYVH = ASSOCIATED (SELF%YVH)
IF (LYVH) THEN
  DO J1 = LBOUND (SELF%YVH, 1), UBOUND (SELF%YVH, 1)
    WRITE (CLIND, '("(",I0,")")') J1
    CALL CRC64_TYPE_SURF_MTL_2D (SELF%YVH (J1), KLUN, CDPATH//'%YVH'//TRIM(CLIND))
  ENDDO
ENDIF

LYPCL = ASSOCIATED (SELF%YPCL)
IF (LYPCL) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YPCL, KLUN, CDPATH//'%YPCL'//TRIM(CLIND))
ENDIF

LYPSL = ASSOCIATED (SELF%YPSL)
IF (LYPSL) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YPSL, KLUN, CDPATH//'%YPSL'//TRIM(CLIND))
ENDIF

LYPCN = ASSOCIATED (SELF%YPCN)
IF (LYPCN) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YPCN, KLUN, CDPATH//'%YPCN'//TRIM(CLIND))
ENDIF

LYPSN = ASSOCIATED (SELF%YPSN)
IF (LYPSN) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YPSN, KLUN, CDPATH//'%YPSN'//TRIM(CLIND))
ENDIF

LYEVA = ASSOCIATED (SELF%YEVA)
IF (LYEVA) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YEVA, KLUN, CDPATH//'%YEVA'//TRIM(CLIND))
ENDIF

END SUBROUTINE

SUBROUTINE WIPE_TYPE_SFL_VCLIH (SELF, LDDELETED, LDFIELDAPI)
USE UTIL_TYPE_SURF_MTL_2D_MOD
IMPLICIT NONE
TYPE (TYPE_SFL_VCLIH), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
INTEGER :: J1
LOGICAL :: LYBCCH, LYEVA, LYPBLH, LYPCL, LYPCN, LYPSL, LYPSN, LYQSH, LYSCCH, LYSPSH
LOGICAL :: LYTCCH, LYVH

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LYTCCH = ASSOCIATED (SELF%YTCCH)
IF (LYTCCH) THEN
  !$ACC EXIT DATA DETACH (SELF%YTCCH)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YTCCH, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YTCCH)
ENDIF

LYSCCH = ASSOCIATED (SELF%YSCCH)
IF (LYSCCH) THEN
  !$ACC EXIT DATA DETACH (SELF%YSCCH)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YSCCH, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YSCCH)
ENDIF

LYBCCH = ASSOCIATED (SELF%YBCCH)
IF (LYBCCH) THEN
  !$ACC EXIT DATA DETACH (SELF%YBCCH)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YBCCH, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YBCCH)
ENDIF

LYPBLH = ASSOCIATED (SELF%YPBLH)
IF (LYPBLH) THEN
  !$ACC EXIT DATA DETACH (SELF%YPBLH)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YPBLH, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YPBLH)
ENDIF

LYSPSH = ASSOCIATED (SELF%YSPSH)
IF (LYSPSH) THEN
  !$ACC EXIT DATA DETACH (SELF%YSPSH)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YSPSH, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YSPSH)
ENDIF

LYQSH = ASSOCIATED (SELF%YQSH)
IF (LYQSH) THEN
  !$ACC EXIT DATA DETACH (SELF%YQSH)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YQSH, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YQSH)
ENDIF

LYVH = ASSOCIATED (SELF%YVH)
IF (LYVH) THEN
  !$ACC EXIT DATA DETACH (SELF%YVH)
  DO J1 = LBOUND (SELF%YVH, 1), UBOUND (SELF%YVH, 1)
    CALL WIPE_TYPE_SURF_MTL_2D (SELF%YVH (J1), LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  ENDDO
  !$ACC EXIT DATA DETACH (SELF%YVH)
ENDIF

LYPCL = ASSOCIATED (SELF%YPCL)
IF (LYPCL) THEN
  !$ACC EXIT DATA DETACH (SELF%YPCL)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YPCL, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YPCL)
ENDIF

LYPSL = ASSOCIATED (SELF%YPSL)
IF (LYPSL) THEN
  !$ACC EXIT DATA DETACH (SELF%YPSL)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YPSL, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YPSL)
ENDIF

LYPCN = ASSOCIATED (SELF%YPCN)
IF (LYPCN) THEN
  !$ACC EXIT DATA DETACH (SELF%YPCN)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YPCN, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YPCN)
ENDIF

LYPSN = ASSOCIATED (SELF%YPSN)
IF (LYPSN) THEN
  !$ACC EXIT DATA DETACH (SELF%YPSN)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YPSN, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YPSN)
ENDIF

LYEVA = ASSOCIATED (SELF%YEVA)
IF (LYEVA) THEN
  !$ACC EXIT DATA DETACH (SELF%YEVA)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YEVA, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YEVA)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE



END MODULE
