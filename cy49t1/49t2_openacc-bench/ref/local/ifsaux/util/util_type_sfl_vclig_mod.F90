MODULE UTIL_TYPE_SFL_VCLIG_MOD

USE SURFACE_FIELDS_MIX, ONLY : TYPE_SFL_VCLIG

INTERFACE SAVE
MODULE PROCEDURE SAVE_TYPE_SFL_VCLIG
END INTERFACE

INTERFACE LOAD
MODULE PROCEDURE LOAD_TYPE_SFL_VCLIG
END INTERFACE

INTERFACE COPY
MODULE PROCEDURE COPY_TYPE_SFL_VCLIG
END INTERFACE

INTERFACE HOST
MODULE PROCEDURE HOST_TYPE_SFL_VCLIG
END INTERFACE

     
INTERFACE CRC64
MODULE PROCEDURE CRC64_TYPE_SFL_VCLIG
END INTERFACE

INTERFACE WIPE
MODULE PROCEDURE WIPE_TYPE_SFL_VCLIG
END INTERFACE



CONTAINS

SUBROUTINE SAVE_TYPE_SFL_VCLIG (SELF, KLUN)
USE UTIL_TYPE_SURF_MTL_2D_MOD
IMPLICIT NONE
TYPE (TYPE_SFL_VCLIG), INTENT (IN), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN
INTEGER :: J1
LOGICAL :: LYCHSS, LYEVAP, LYICFR, LYIRUP, LYSOUP, LYTAUX, LYTAUY, LYVG
LYICFR = ASSOCIATED (SELF%YICFR)
WRITE (KLUN) LYICFR
IF (LYICFR) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YICFR, KLUN)
ENDIF
LYSOUP = ASSOCIATED (SELF%YSOUP)
WRITE (KLUN) LYSOUP
IF (LYSOUP) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YSOUP, KLUN)
ENDIF
LYIRUP = ASSOCIATED (SELF%YIRUP)
WRITE (KLUN) LYIRUP
IF (LYIRUP) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YIRUP, KLUN)
ENDIF
LYCHSS = ASSOCIATED (SELF%YCHSS)
WRITE (KLUN) LYCHSS
IF (LYCHSS) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YCHSS, KLUN)
ENDIF
LYEVAP = ASSOCIATED (SELF%YEVAP)
WRITE (KLUN) LYEVAP
IF (LYEVAP) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YEVAP, KLUN)
ENDIF
LYTAUX = ASSOCIATED (SELF%YTAUX)
WRITE (KLUN) LYTAUX
IF (LYTAUX) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YTAUX, KLUN)
ENDIF
LYTAUY = ASSOCIATED (SELF%YTAUY)
WRITE (KLUN) LYTAUY
IF (LYTAUY) THEN
  CALL SAVE_TYPE_SURF_MTL_2D (SELF%YTAUY, KLUN)
ENDIF
LYVG = ASSOCIATED (SELF%YVG)
WRITE (KLUN) LYVG
IF (LYVG) THEN
  WRITE (KLUN) LBOUND (SELF%YVG)
  WRITE (KLUN) UBOUND (SELF%YVG)
  DO J1 = LBOUND (SELF%YVG, 1), UBOUND (SELF%YVG, 1)
    CALL SAVE_TYPE_SURF_MTL_2D (SELF%YVG (J1), KLUN)
  ENDDO
ENDIF
END SUBROUTINE

SUBROUTINE LOAD_TYPE_SFL_VCLIG (SELF, KLUN)
USE UTIL_TYPE_SURF_MTL_2D_MOD

IMPLICIT NONE
TYPE (TYPE_SFL_VCLIG), INTENT (OUT), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN
INTEGER :: J1
INTEGER :: IL1(1), IU1(1)
LOGICAL :: LYCHSS, LYEVAP, LYICFR, LYIRUP, LYSOUP, LYTAUX, LYTAUY, LYVG
READ (KLUN) LYICFR
IF (LYICFR) THEN
  ALLOCATE (SELF%YICFR)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YICFR, KLUN)
ELSE
  NULLIFY (SELF%YICFR)
ENDIF
READ (KLUN) LYSOUP
IF (LYSOUP) THEN
  ALLOCATE (SELF%YSOUP)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YSOUP, KLUN)
ELSE
  NULLIFY (SELF%YSOUP)
ENDIF
READ (KLUN) LYIRUP
IF (LYIRUP) THEN
  ALLOCATE (SELF%YIRUP)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YIRUP, KLUN)
ELSE
  NULLIFY (SELF%YIRUP)
ENDIF
READ (KLUN) LYCHSS
IF (LYCHSS) THEN
  ALLOCATE (SELF%YCHSS)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YCHSS, KLUN)
ELSE
  NULLIFY (SELF%YCHSS)
ENDIF
READ (KLUN) LYEVAP
IF (LYEVAP) THEN
  ALLOCATE (SELF%YEVAP)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YEVAP, KLUN)
ELSE
  NULLIFY (SELF%YEVAP)
ENDIF
READ (KLUN) LYTAUX
IF (LYTAUX) THEN
  ALLOCATE (SELF%YTAUX)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YTAUX, KLUN)
ELSE
  NULLIFY (SELF%YTAUX)
ENDIF
READ (KLUN) LYTAUY
IF (LYTAUY) THEN
  ALLOCATE (SELF%YTAUY)
  CALL LOAD_TYPE_SURF_MTL_2D (SELF%YTAUY, KLUN)
ELSE
  NULLIFY (SELF%YTAUY)
ENDIF
READ (KLUN) LYVG
IF (LYVG) THEN
  READ (KLUN) IL1
  READ (KLUN) IU1
  ALLOCATE (SELF%YVG (IL1(1):IU1(1)))
  DO J1 = LBOUND (SELF%YVG, 1), UBOUND (SELF%YVG, 1)
    CALL LOAD_TYPE_SURF_MTL_2D (SELF%YVG (J1), KLUN)
  ENDDO
ELSE
  NULLIFY (SELF%YVG)
ENDIF
END SUBROUTINE


SUBROUTINE COPY_TYPE_SFL_VCLIG (SELF, LDCREATED, LDFIELDAPI)
USE UTIL_TYPE_SURF_MTL_2D_MOD
IMPLICIT NONE
TYPE (TYPE_SFL_VCLIG), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDCREATED, LDFIELDAPI
LOGICAL :: LLCREATED
LOGICAL :: LLFIELDAPI
INTEGER :: J1
LOGICAL :: LYCHSS, LYEVAP, LYICFR, LYIRUP, LYSOUP, LYTAUX, LYTAUY, LYVG

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LLCREATED = .FALSE.
IF (PRESENT (LDCREATED)) THEN
  LLCREATED = LDCREATED
ENDIF
IF (.NOT. LLCREATED) THEN
  !$ACC ENTER DATA CREATE (SELF)
  !$ACC UPDATE DEVICE (SELF)
ENDIF
LYICFR = ASSOCIATED (SELF%YICFR)
IF (LYICFR) THEN
  !$ACC ENTER DATA CREATE (SELF%YICFR)
  !$ACC UPDATE DEVICE (SELF%YICFR)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YICFR, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YICFR)
ENDIF

LYSOUP = ASSOCIATED (SELF%YSOUP)
IF (LYSOUP) THEN
  !$ACC ENTER DATA CREATE (SELF%YSOUP)
  !$ACC UPDATE DEVICE (SELF%YSOUP)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YSOUP, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YSOUP)
ENDIF

LYIRUP = ASSOCIATED (SELF%YIRUP)
IF (LYIRUP) THEN
  !$ACC ENTER DATA CREATE (SELF%YIRUP)
  !$ACC UPDATE DEVICE (SELF%YIRUP)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YIRUP, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YIRUP)
ENDIF

LYCHSS = ASSOCIATED (SELF%YCHSS)
IF (LYCHSS) THEN
  !$ACC ENTER DATA CREATE (SELF%YCHSS)
  !$ACC UPDATE DEVICE (SELF%YCHSS)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YCHSS, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YCHSS)
ENDIF

LYEVAP = ASSOCIATED (SELF%YEVAP)
IF (LYEVAP) THEN
  !$ACC ENTER DATA CREATE (SELF%YEVAP)
  !$ACC UPDATE DEVICE (SELF%YEVAP)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YEVAP, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YEVAP)
ENDIF

LYTAUX = ASSOCIATED (SELF%YTAUX)
IF (LYTAUX) THEN
  !$ACC ENTER DATA CREATE (SELF%YTAUX)
  !$ACC UPDATE DEVICE (SELF%YTAUX)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YTAUX, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YTAUX)
ENDIF

LYTAUY = ASSOCIATED (SELF%YTAUY)
IF (LYTAUY) THEN
  !$ACC ENTER DATA CREATE (SELF%YTAUY)
  !$ACC UPDATE DEVICE (SELF%YTAUY)
  CALL COPY_TYPE_SURF_MTL_2D (SELF%YTAUY, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%YTAUY)
ENDIF

LYVG = ASSOCIATED (SELF%YVG)
IF (LYVG) THEN
  !$ACC ENTER DATA CREATE (SELF%YVG)
  !$ACC UPDATE DEVICE (SELF%YVG)
  DO J1 = LBOUND (SELF%YVG, 1), UBOUND (SELF%YVG, 1)
    CALL COPY_TYPE_SURF_MTL_2D (SELF%YVG (J1), LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  ENDDO
  !$ACC ENTER DATA ATTACH (SELF%YVG)
ENDIF

END SUBROUTINE

SUBROUTINE HOST_TYPE_SFL_VCLIG (SELF)
USE UTIL_TYPE_SURF_MTL_2D_MOD

IMPLICIT NONE
TYPE (TYPE_SFL_VCLIG), TARGET :: SELF
INTEGER :: J1
LOGICAL :: LYCHSS, LYEVAP, LYICFR, LYIRUP, LYSOUP, LYTAUX, LYTAUY, LYVG
LYICFR = ASSOCIATED (SELF%YICFR)
IF (LYICFR) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YICFR)
ENDIF

LYSOUP = ASSOCIATED (SELF%YSOUP)
IF (LYSOUP) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YSOUP)
ENDIF

LYIRUP = ASSOCIATED (SELF%YIRUP)
IF (LYIRUP) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YIRUP)
ENDIF

LYCHSS = ASSOCIATED (SELF%YCHSS)
IF (LYCHSS) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YCHSS)
ENDIF

LYEVAP = ASSOCIATED (SELF%YEVAP)
IF (LYEVAP) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YEVAP)
ENDIF

LYTAUX = ASSOCIATED (SELF%YTAUX)
IF (LYTAUX) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YTAUX)
ENDIF

LYTAUY = ASSOCIATED (SELF%YTAUY)
IF (LYTAUY) THEN
  CALL HOST_TYPE_SURF_MTL_2D (SELF%YTAUY)
ENDIF

LYVG = ASSOCIATED (SELF%YVG)
IF (LYVG) THEN
  DO J1 = LBOUND (SELF%YVG, 1), UBOUND (SELF%YVG, 1)
    CALL HOST_TYPE_SURF_MTL_2D (SELF%YVG (J1))
  ENDDO
ENDIF

END SUBROUTINE

     

SUBROUTINE CRC64_TYPE_SFL_VCLIG (SELF, KLUN, CDPATH)
USE UTIL_TYPE_SURF_MTL_2D_MOD
USE CRC64_INTRINSIC, ONLY : FCRC64 => CRC64

IMPLICIT NONE
TYPE (TYPE_SFL_VCLIG), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN
CHARACTER(LEN=*), INTENT (IN) :: CDPATH
CHARACTER(LEN=128) :: CLIND
INTEGER :: J1
LOGICAL :: LYCHSS, LYEVAP, LYICFR, LYIRUP, LYSOUP, LYTAUX, LYTAUY, LYVG
LYICFR = ASSOCIATED (SELF%YICFR)
IF (LYICFR) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YICFR, KLUN, CDPATH//'%YICFR'//TRIM(CLIND))
ENDIF

LYSOUP = ASSOCIATED (SELF%YSOUP)
IF (LYSOUP) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YSOUP, KLUN, CDPATH//'%YSOUP'//TRIM(CLIND))
ENDIF

LYIRUP = ASSOCIATED (SELF%YIRUP)
IF (LYIRUP) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YIRUP, KLUN, CDPATH//'%YIRUP'//TRIM(CLIND))
ENDIF

LYCHSS = ASSOCIATED (SELF%YCHSS)
IF (LYCHSS) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YCHSS, KLUN, CDPATH//'%YCHSS'//TRIM(CLIND))
ENDIF

LYEVAP = ASSOCIATED (SELF%YEVAP)
IF (LYEVAP) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YEVAP, KLUN, CDPATH//'%YEVAP'//TRIM(CLIND))
ENDIF

LYTAUX = ASSOCIATED (SELF%YTAUX)
IF (LYTAUX) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YTAUX, KLUN, CDPATH//'%YTAUX'//TRIM(CLIND))
ENDIF

LYTAUY = ASSOCIATED (SELF%YTAUY)
IF (LYTAUY) THEN
  CLIND = ''
  CALL CRC64_TYPE_SURF_MTL_2D (SELF%YTAUY, KLUN, CDPATH//'%YTAUY'//TRIM(CLIND))
ENDIF

LYVG = ASSOCIATED (SELF%YVG)
IF (LYVG) THEN
  DO J1 = LBOUND (SELF%YVG, 1), UBOUND (SELF%YVG, 1)
    WRITE (CLIND, '("(",I0,")")') J1
    CALL CRC64_TYPE_SURF_MTL_2D (SELF%YVG (J1), KLUN, CDPATH//'%YVG'//TRIM(CLIND))
  ENDDO
ENDIF

END SUBROUTINE

SUBROUTINE WIPE_TYPE_SFL_VCLIG (SELF, LDDELETED, LDFIELDAPI)
USE UTIL_TYPE_SURF_MTL_2D_MOD
IMPLICIT NONE
TYPE (TYPE_SFL_VCLIG), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
INTEGER :: J1
LOGICAL :: LYCHSS, LYEVAP, LYICFR, LYIRUP, LYSOUP, LYTAUX, LYTAUY, LYVG

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LYICFR = ASSOCIATED (SELF%YICFR)
IF (LYICFR) THEN
  !$ACC EXIT DATA DETACH (SELF%YICFR)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YICFR, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YICFR)
ENDIF

LYSOUP = ASSOCIATED (SELF%YSOUP)
IF (LYSOUP) THEN
  !$ACC EXIT DATA DETACH (SELF%YSOUP)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YSOUP, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YSOUP)
ENDIF

LYIRUP = ASSOCIATED (SELF%YIRUP)
IF (LYIRUP) THEN
  !$ACC EXIT DATA DETACH (SELF%YIRUP)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YIRUP, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YIRUP)
ENDIF

LYCHSS = ASSOCIATED (SELF%YCHSS)
IF (LYCHSS) THEN
  !$ACC EXIT DATA DETACH (SELF%YCHSS)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YCHSS, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YCHSS)
ENDIF

LYEVAP = ASSOCIATED (SELF%YEVAP)
IF (LYEVAP) THEN
  !$ACC EXIT DATA DETACH (SELF%YEVAP)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YEVAP, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YEVAP)
ENDIF

LYTAUX = ASSOCIATED (SELF%YTAUX)
IF (LYTAUX) THEN
  !$ACC EXIT DATA DETACH (SELF%YTAUX)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YTAUX, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YTAUX)
ENDIF

LYTAUY = ASSOCIATED (SELF%YTAUY)
IF (LYTAUY) THEN
  !$ACC EXIT DATA DETACH (SELF%YTAUY)
  CALL WIPE_TYPE_SURF_MTL_2D (SELF%YTAUY, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%YTAUY)
ENDIF

LYVG = ASSOCIATED (SELF%YVG)
IF (LYVG) THEN
  !$ACC EXIT DATA DETACH (SELF%YVG)
  DO J1 = LBOUND (SELF%YVG, 1), UBOUND (SELF%YVG, 1)
    CALL WIPE_TYPE_SURF_MTL_2D (SELF%YVG (J1), LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  ENDDO
  !$ACC EXIT DATA DETACH (SELF%YVG)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE



END MODULE
