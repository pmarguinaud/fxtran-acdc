MODULE UTIL_TECND_MOD

USE YOECND, ONLY : TECND

INTERFACE SAVE
MODULE PROCEDURE SAVE_TECND
END INTERFACE

INTERFACE LOAD
MODULE PROCEDURE LOAD_TECND
END INTERFACE

INTERFACE COPY
MODULE PROCEDURE COPY_TECND
END INTERFACE

INTERFACE HOST
MODULE PROCEDURE HOST_TECND
END INTERFACE

     
INTERFACE CRC64
MODULE PROCEDURE CRC64_TECND
END INTERFACE

INTERFACE WIPE
MODULE PROCEDURE WIPE_TECND
END INTERFACE

INTERFACE SIZE
MODULE PROCEDURE SIZE_TECND
END INTERFACE


CONTAINS

SUBROUTINE SAVE_TECND (KLUN, YD)

IMPLICIT NONE
INTEGER, INTENT (IN) :: KLUN
TYPE (TECND), INTENT (IN), TARGET :: YD

WRITE (KLUN) YD%REPFLM
WRITE (KLUN) YD%REPQMI
END SUBROUTINE

SUBROUTINE LOAD_TECND (KLUN, YD)
USE PARKIND1, ONLY : JPRD

IMPLICIT NONE
INTEGER, INTENT (IN) :: KLUN
TYPE (TECND), INTENT (OUT), TARGET :: YD
REAL(KIND=JPRD) :: ZTMP0
READ (KLUN) ZTMP0
YD%REPFLM = ZTMP0
READ (KLUN) ZTMP0
YD%REPQMI = ZTMP0
END SUBROUTINE


SUBROUTINE COPY_TECND (YD, LDCREATED, LDFIELDAPI)

IMPLICIT NONE
TYPE (TECND), INTENT (IN), TARGET :: YD
LOGICAL, OPTIONAL, INTENT (IN) :: LDCREATED, LDFIELDAPI
LOGICAL :: LLCREATED
LOGICAL :: LLFIELDAPI

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LLCREATED = .FALSE.
IF (PRESENT (LDCREATED)) THEN
  LLCREATED = LDCREATED
ENDIF
IF (.NOT. LLCREATED) THEN
  !$ACC ENTER DATA CREATE (YD)
  !$ACC UPDATE DEVICE (YD)
ENDIF


END SUBROUTINE

SUBROUTINE HOST_TECND (YD)

IMPLICIT NONE
TYPE (TECND), TARGET :: YD



END SUBROUTINE

     

SUBROUTINE CRC64_TECND (YD, KLUN, CDPATH)
USE CRC64_INTRINSIC, ONLY : FCRC64 => CRC64

IMPLICIT NONE
TYPE (TECND), TARGET :: YD
INTEGER, INTENT (IN) :: KLUN
CHARACTER(LEN=*), INTENT (IN) :: CDPATH
CHARACTER(LEN=128) :: CLIND

WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (YD%REPFLM), CDPATH//'%REPFLM'

WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (YD%REPQMI), CDPATH//'%REPQMI'

END SUBROUTINE

SUBROUTINE WIPE_TECND (YD, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
TYPE (TECND), INTENT (IN), TARGET :: YD
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF


LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (YD)
ENDIF
END SUBROUTINE

INTEGER*8 FUNCTION SIZE_TECND (YD, CDPATH, LDPRINT) RESULT (KSIZE)

IMPLICIT NONE
TYPE (TECND),     INTENT (IN), TARGET :: YD
CHARACTER(LEN=*), INTENT (IN), OPTIONAL :: CDPATH
LOGICAL,          INTENT (IN), OPTIONAL :: LDPRINT
INTEGER*8 :: ISIZE, JSIZE
LOGICAL :: LLPRINT
CHARACTER(LEN=128) :: CLPATH

LLPRINT = .FALSE.
IF (PRESENT (LDPRINT)) THEN
  LLPRINT = LDPRINT
ENDIF
CLPATH=''
IF (PRESENT (CDPATH)) THEN
  CLPATH = CDPATH
ENDIF
KSIZE = 0
ISIZE = KIND (YD%REPFLM)
IF (LLPRINT) THEN
  WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
  WRITE (*, *) TRIM (CLPATH)//'%REPFLM'
ENDIF
KSIZE = KSIZE + ISIZE
ISIZE = KIND (YD%REPQMI)
IF (LLPRINT) THEN
  WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
  WRITE (*, *) TRIM (CLPATH)//'%REPQMI'
ENDIF
KSIZE = KSIZE + ISIZE
END FUNCTION

END MODULE
