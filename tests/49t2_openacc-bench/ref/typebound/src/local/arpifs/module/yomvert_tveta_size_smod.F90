SUBMODULE (YOMVERT) YOMVERT_TVETA_SIZE_SMOD

IMPLICIT NONE

CONTAINS

MODULE FUNCTION ACDC_SIZE_TVETA (SELF, CDPATH, LDPRINT) RESULT (KSIZE)

IMPLICIT NONE
CLASS (TVETA),     INTENT (IN), TARGET :: SELF
CHARACTER(LEN=*), INTENT (IN), OPTIONAL :: CDPATH
LOGICAL,          INTENT (IN), OPTIONAL :: LDPRINT
INTEGER*8 :: KSIZE
INTEGER*8 :: ISIZE, JSIZE
LOGICAL :: LLPRINT
CHARACTER(LEN=128) :: CLPATH
LOGICAL :: LVDETA_RATIO, LVETAF, LVETAH, LVETA_H, LVETA_LAITRIQM3D, LVFE_ETAF, LVFE_ETAH, LVFE_RDETAH, LVRDETAH, LVWEI
LLPRINT = .FALSE.
IF (PRESENT (LDPRINT)) THEN
  LLPRINT = LDPRINT
ENDIF
CLPATH=''
IF (PRESENT (CDPATH)) THEN
  CLPATH = CDPATH
ENDIF
KSIZE = 0
LVETAH = ALLOCATED (SELF%VETAH)
IF (LVETAH) THEN
  ISIZE = KIND (SELF%VETAH) * SIZE (SELF%VETAH)
  IF (LLPRINT) THEN
    WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
    WRITE (*, *) TRIM (CLPATH)//'%VETAH'
  ENDIF
  KSIZE = KSIZE + ISIZE
ENDIF
LVFE_ETAH = ALLOCATED (SELF%VFE_ETAH)
IF (LVFE_ETAH) THEN
  ISIZE = KIND (SELF%VFE_ETAH) * SIZE (SELF%VFE_ETAH)
  IF (LLPRINT) THEN
    WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
    WRITE (*, *) TRIM (CLPATH)//'%VFE_ETAH'
  ENDIF
  KSIZE = KSIZE + ISIZE
ENDIF
LVETAF = ALLOCATED (SELF%VETAF)
IF (LVETAF) THEN
  ISIZE = KIND (SELF%VETAF) * SIZE (SELF%VETAF)
  IF (LLPRINT) THEN
    WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
    WRITE (*, *) TRIM (CLPATH)//'%VETAF'
  ENDIF
  KSIZE = KSIZE + ISIZE
ENDIF
LVFE_ETAF = ALLOCATED (SELF%VFE_ETAF)
IF (LVFE_ETAF) THEN
  ISIZE = KIND (SELF%VFE_ETAF) * SIZE (SELF%VFE_ETAF)
  IF (LLPRINT) THEN
    WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
    WRITE (*, *) TRIM (CLPATH)//'%VFE_ETAF'
  ENDIF
  KSIZE = KSIZE + ISIZE
ENDIF
LVFE_RDETAH = ALLOCATED (SELF%VFE_RDETAH)
IF (LVFE_RDETAH) THEN
  ISIZE = KIND (SELF%VFE_RDETAH) * SIZE (SELF%VFE_RDETAH)
  IF (LLPRINT) THEN
    WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
    WRITE (*, *) TRIM (CLPATH)//'%VFE_RDETAH'
  ENDIF
  KSIZE = KSIZE + ISIZE
ENDIF
LVDETA_RATIO = ALLOCATED (SELF%VDETA_RATIO)
IF (LVDETA_RATIO) THEN
  ISIZE = KIND (SELF%VDETA_RATIO) * SIZE (SELF%VDETA_RATIO)
  IF (LLPRINT) THEN
    WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
    WRITE (*, *) TRIM (CLPATH)//'%VDETA_RATIO'
  ENDIF
  KSIZE = KSIZE + ISIZE
ENDIF
LVRDETAH = ALLOCATED (SELF%VRDETAH)
IF (LVRDETAH) THEN
  ISIZE = KIND (SELF%VRDETAH) * SIZE (SELF%VRDETAH)
  IF (LLPRINT) THEN
    WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
    WRITE (*, *) TRIM (CLPATH)//'%VRDETAH'
  ENDIF
  KSIZE = KSIZE + ISIZE
ENDIF
LVWEI = ALLOCATED (SELF%VWEI)
IF (LVWEI) THEN
  ISIZE = KIND (SELF%VWEI) * SIZE (SELF%VWEI)
  IF (LLPRINT) THEN
    WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
    WRITE (*, *) TRIM (CLPATH)//'%VWEI'
  ENDIF
  KSIZE = KSIZE + ISIZE
ENDIF
LVETA_H = ALLOCATED (SELF%VETA_H)
IF (LVETA_H) THEN
  ISIZE = KIND (SELF%VETA_H) * SIZE (SELF%VETA_H)
  IF (LLPRINT) THEN
    WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
    WRITE (*, *) TRIM (CLPATH)//'%VETA_H'
  ENDIF
  KSIZE = KSIZE + ISIZE
ENDIF
LVETA_LAITRIQM3D = ALLOCATED (SELF%VETA_LAITRIQM3D)
IF (LVETA_LAITRIQM3D) THEN
  ISIZE = KIND (SELF%VETA_LAITRIQM3D) * SIZE (SELF%VETA_LAITRIQM3D)
  IF (LLPRINT) THEN
    WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
    WRITE (*, *) TRIM (CLPATH)//'%VETA_LAITRIQM3D'
  ENDIF
  KSIZE = KSIZE + ISIZE
ENDIF
END FUNCTION

END SUBMODULE
