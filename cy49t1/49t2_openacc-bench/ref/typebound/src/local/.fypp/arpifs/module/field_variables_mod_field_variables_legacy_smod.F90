SUBMODULE (FIELD_VARIABLES_MOD) FIELD_VARIABLES_MOD_FIELD_VARIABLES_LEGACY_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE LEGACY_FIELD_VARIABLES (SELF, KADDRL, KADDRU, KDIR)

IMPLICIT NONE
CLASS (FIELD_VARIABLES), TARGET :: SELF
INTEGER*8, INTENT (IN) :: KADDRL
INTEGER*8, INTENT (IN) :: KADDRU
INTEGER, INTENT (IN) :: KDIR
INTEGER :: J1
LOGICAL :: LAERAOT, LAERLISI, LAERO, LAEROCLIM, LAEROUT, LCHEM, LEDRP, LEMIS3D, LERA40, LEXT
LOGICAL :: LEZDIAG, LFORC, LGFL_PTR, LGHG, LLIMA, LNOGW, LSLDIA, LUVP
CALL SELF%U%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%V%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%T%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%DIV%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%VOR%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SPD%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SVD%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CSPDNL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CSPNL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CSVDPT%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CTNL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CTPT%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CUNL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CUPT%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CVNL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CVPT%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CVWVNL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%DPHI%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%EDOT%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%GW%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%NHX%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%NHY%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SGRTL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SGRTM%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SPDNL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SPDNL_SI%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SPNL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SPNL_SI%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SVDNL_SI%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%TNL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%TNL_SI%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%UNL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%UNL_SI%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%VNL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%VNL_SI%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%VWVNL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CURHS%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CVRHS%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CTRHS%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CSPDRHS%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CSVDRHS%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%NHXNL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CNHXNL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%PCF_U%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%PCF_V%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%PCF_T%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%PCF_SPD%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%PCF_SVD%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%PCF_NHX%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%PCF_SP%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%PCF_CP%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%PCF_UP%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%PCF_VP%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%PCF_TP%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SP%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SPNL2%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%PREHYDS%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%DBBC%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CSPPT%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CSPNL2%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CSPRHS%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%GWS%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%PCF_BBC%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%PCF_DPHI%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%PCF_GWS%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%Q%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%I%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%L%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%IMF%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%LMF%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%LCONV%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%ICONV%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%RCONV%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SCONV%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%IRAD%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%LRAD%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%S%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%R%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%G%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%H%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%TKE%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%TTE%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%EFB1%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%EFB2%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%EFB3%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%A%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%AMF%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%O3%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SRC%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%MXL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SHTUR%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%FQTUR%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%FSTUR%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CPF%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SPF%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CVGQ%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%QVA%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

LGHG = ALLOCATED (SELF%GHG)
IF (LGHG) THEN
  DO J1 = LBOUND (SELF%GHG, 1), UBOUND (SELF%GHG, 1)
    CALL SELF%GHG (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LCHEM = ALLOCATED (SELF%CHEM)
IF (LCHEM) THEN
  DO J1 = LBOUND (SELF%CHEM, 1), UBOUND (SELF%CHEM, 1)
    CALL SELF%CHEM (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LAERO = ALLOCATED (SELF%AERO)
IF (LAERO) THEN
  DO J1 = LBOUND (SELF%AERO, 1), UBOUND (SELF%AERO, 1)
    CALL SELF%AERO (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

CALL SELF%LRCH4%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

LFORC = ALLOCATED (SELF%FORC)
IF (LFORC) THEN
  DO J1 = LBOUND (SELF%FORC, 1), UBOUND (SELF%FORC, 1)
    CALL SELF%FORC (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LEZDIAG = ALLOCATED (SELF%EZDIAG)
IF (LEZDIAG) THEN
  DO J1 = LBOUND (SELF%EZDIAG, 1), UBOUND (SELF%EZDIAG, 1)
    CALL SELF%EZDIAG (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LERA40 = ALLOCATED (SELF%ERA40)
IF (LERA40) THEN
  DO J1 = LBOUND (SELF%ERA40, 1), UBOUND (SELF%ERA40, 1)
    CALL SELF%ERA40 (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LNOGW = ALLOCATED (SELF%NOGW)
IF (LNOGW) THEN
  DO J1 = LBOUND (SELF%NOGW, 1), UBOUND (SELF%NOGW, 1)
    CALL SELF%NOGW (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LEMIS3D = ALLOCATED (SELF%EMIS3D)
IF (LEMIS3D) THEN
  DO J1 = LBOUND (SELF%EMIS3D, 1), UBOUND (SELF%EMIS3D, 1)
    CALL SELF%EMIS3D (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LEDRP = ALLOCATED (SELF%EDRP)
IF (LEDRP) THEN
  DO J1 = LBOUND (SELF%EDRP, 1), UBOUND (SELF%EDRP, 1)
    CALL SELF%EDRP (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LSLDIA = ALLOCATED (SELF%SLDIA)
IF (LSLDIA) THEN
  DO J1 = LBOUND (SELF%SLDIA, 1), UBOUND (SELF%SLDIA, 1)
    CALL SELF%SLDIA (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LAERAOT = ALLOCATED (SELF%AERAOT)
IF (LAERAOT) THEN
  DO J1 = LBOUND (SELF%AERAOT, 1), UBOUND (SELF%AERAOT, 1)
    CALL SELF%AERAOT (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LAERLISI = ALLOCATED (SELF%AERLISI)
IF (LAERLISI) THEN
  DO J1 = LBOUND (SELF%AERLISI, 1), UBOUND (SELF%AERLISI, 1)
    CALL SELF%AERLISI (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LAEROUT = ALLOCATED (SELF%AEROUT)
IF (LAEROUT) THEN
  DO J1 = LBOUND (SELF%AEROUT, 1), UBOUND (SELF%AEROUT, 1)
    CALL SELF%AEROUT (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LAEROCLIM = ALLOCATED (SELF%AEROCLIM)
IF (LAEROCLIM) THEN
  DO J1 = LBOUND (SELF%AEROCLIM, 1), UBOUND (SELF%AEROCLIM, 1)
    CALL SELF%AEROCLIM (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LUVP = ALLOCATED (SELF%UVP)
IF (LUVP) THEN
  DO J1 = LBOUND (SELF%UVP, 1), UBOUND (SELF%UVP, 1)
    CALL SELF%UVP (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

CALL SELF%PHYCTY%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%RSPEC%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%SDSAT%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%CVV%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%RKTH%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%RKTQV%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%RKTQC%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%UOM%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%UAL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%DOM%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%DAL%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%UEN%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%UNEBH%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

LLIMA = ALLOCATED (SELF%LIMA)
IF (LLIMA) THEN
  DO J1 = LBOUND (SELF%LIMA, 1), UBOUND (SELF%LIMA, 1)
    CALL SELF%LIMA (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

CALL SELF%FSD%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

LEXT = ALLOCATED (SELF%EXT)
IF (LEXT) THEN
  DO J1 = LBOUND (SELF%EXT, 1), UBOUND (SELF%EXT, 1)
    CALL SELF%EXT (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

CALL SELF%GEOMETRY%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%ECPHYS%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%RADIATION%LEGACY (KADDRL, KADDRU, KDIR=KDIR)

LGFL_PTR = ALLOCATED (SELF%GFL_PTR)
IF (LGFL_PTR) THEN
  DO J1 = LBOUND (SELF%GFL_PTR, 1), UBOUND (SELF%GFL_PTR, 1)
    CALL SELF%GFL_PTR (J1)%LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

END SUBROUTINE

END SUBMODULE
