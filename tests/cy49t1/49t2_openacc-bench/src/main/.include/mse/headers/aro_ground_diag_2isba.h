INTERFACE
 SUBROUTINE ARO_GROUND_DIAG_2ISBA( KBL, KLON, KST, KEND, &
 & KDGUNG, KDGUXG, KDLUNG, KDLUXG, PINDX, PINDY, &
 & PLSM, PARG, PSAB, PD2, PTSN, PWSN, &
 & PTP2, PWS2, PWP2, PWSI2, PWPI2,PWR2, PSNA2, PSND2, PHV2 )
 USE PARKIND1, ONLY : JPRB, JPIM
INTEGER(KIND=JPIM), INTENT(IN) :: KBL
INTEGER(KIND=JPIM), INTENT(IN) :: KLON
INTEGER(KIND=JPIM), INTENT(IN) :: KST
INTEGER(KIND=JPIM), INTENT(IN) :: KEND
INTEGER(KIND=JPIM), INTENT(IN) :: KDGUNG, KDGUXG, KDLUNG, KDLUXG
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(IN) :: PLSM
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(IN) :: PSAB
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(IN) :: PARG
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(IN) :: PD2
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(IN) :: PTSN
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(IN) :: PWSN
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(IN) :: PINDX
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(IN) :: PINDY
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(OUT) :: PTP2
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(OUT) :: PWS2
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(OUT) :: PWP2
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(OUT) :: PWSI2
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(OUT) :: PWPI2
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(OUT) :: PWR2
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(OUT) :: PSNA2
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(OUT) :: PSND2
REAL(KIND=JPRB), DIMENSION(KLON), INTENT(OUT) :: PHV2
END SUBROUTINE ARO_GROUND_DIAG_2ISBA
END INTERFACE
