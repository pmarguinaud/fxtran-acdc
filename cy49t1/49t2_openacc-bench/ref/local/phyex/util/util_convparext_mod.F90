MODULE UTIL_CONVPAREXT_MOD

USE MODD_CONVPAREXT, ONLY : CONVPAREXT

INTERFACE SAVE
MODULE PROCEDURE SAVE_CONVPAREXT
END INTERFACE

INTERFACE LOAD
MODULE PROCEDURE LOAD_CONVPAREXT
END INTERFACE

INTERFACE COPY
MODULE PROCEDURE COPY_CONVPAREXT
END INTERFACE

INTERFACE HOST
MODULE PROCEDURE HOST_CONVPAREXT
END INTERFACE

     
INTERFACE CRC64
MODULE PROCEDURE CRC64_CONVPAREXT
END INTERFACE

INTERFACE WIPE
MODULE PROCEDURE WIPE_CONVPAREXT
END INTERFACE

INTERFACE SIZE
MODULE PROCEDURE SIZE_CONVPAREXT
END INTERFACE


CONTAINS

SUBROUTINE SAVE_CONVPAREXT (SELF, KLUN)

IMPLICIT NONE
TYPE (CONVPAREXT), INTENT (IN), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN

WRITE (KLUN) SELF%JCVEXB
WRITE (KLUN) SELF%JCVEXT
END SUBROUTINE

SUBROUTINE LOAD_CONVPAREXT (SELF, KLUN)

IMPLICIT NONE
TYPE (CONVPAREXT), INTENT (OUT), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN

READ (KLUN) SELF%JCVEXB
READ (KLUN) SELF%JCVEXT
END SUBROUTINE


SUBROUTINE COPY_CONVPAREXT (SELF, LDCREATED, LDFIELDAPI)

IMPLICIT NONE
TYPE (CONVPAREXT), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDCREATED, LDFIELDAPI
LOGICAL :: LLCREATED
LOGICAL :: LLFIELDAPI

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LLCREATED = .FALSE.
IF (PRESENT (LDCREATED)) THEN
  LLCREATED = LDCREATED
ENDIF
IF (.NOT. LLCREATED) THEN
  !$ACC ENTER DATA CREATE (SELF)
  !$ACC UPDATE DEVICE (SELF)
ENDIF

END SUBROUTINE

SUBROUTINE HOST_CONVPAREXT (SELF)

IMPLICIT NONE
TYPE (CONVPAREXT), TARGET :: SELF


END SUBROUTINE

     

SUBROUTINE CRC64_CONVPAREXT (SELF, KLUN, CDPATH)
USE CRC64_INTRINSIC, ONLY : FCRC64 => CRC64

IMPLICIT NONE
TYPE (CONVPAREXT), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN
CHARACTER(LEN=*), INTENT (IN) :: CDPATH
CHARACTER(LEN=128) :: CLIND
WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%JCVEXB), CDPATH//'%JCVEXB'

WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%JCVEXT), CDPATH//'%JCVEXT'

END SUBROUTINE

SUBROUTINE WIPE_CONVPAREXT (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
TYPE (CONVPAREXT), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

FUNCTION SIZE_CONVPAREXT (SELF, CDPATH, LDPRINT) RESULT (KSIZE)

IMPLICIT NONE
TYPE (CONVPAREXT),     INTENT (IN), TARGET :: SELF
CHARACTER(LEN=*), INTENT (IN), OPTIONAL :: CDPATH
LOGICAL,          INTENT (IN), OPTIONAL :: LDPRINT
INTEGER*8 :: KSIZE
INTEGER*8 :: ISIZE, JSIZE
LOGICAL :: LLPRINT
CHARACTER(LEN=128) :: CLPATH

LLPRINT = .FALSE.
IF (PRESENT (LDPRINT)) THEN
  LLPRINT = LDPRINT
ENDIF
CLPATH=''
IF (PRESENT (CDPATH)) THEN
  CLPATH = CDPATH
ENDIF
KSIZE = 0
ISIZE = KIND (SELF%JCVEXB)
IF (LLPRINT) THEN
  WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
  WRITE (*, *) TRIM (CLPATH)//'%JCVEXB'
ENDIF
KSIZE = KSIZE + ISIZE
ISIZE = KIND (SELF%JCVEXT)
IF (LLPRINT) THEN
  WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
  WRITE (*, *) TRIM (CLPATH)//'%JCVEXT'
ENDIF
KSIZE = KSIZE + ISIZE
END FUNCTION

END MODULE
