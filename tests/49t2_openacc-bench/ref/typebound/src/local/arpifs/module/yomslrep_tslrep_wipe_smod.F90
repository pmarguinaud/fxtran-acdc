SUBMODULE (YOMSLREP) YOMSLREP_TSLREP_WIPE_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_WIPE_TSLREP (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
CLASS (TSLREP), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LLADCORE, LNADCORE, LNADMAP, LRSASIGN
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF

LNADMAP = ALLOCATED (SELF%NADMAP)
IF (LNADMAP) THEN
  !$ACC EXIT DATA DETACH (SELF%NADMAP)
  !$ACC EXIT DATA DELETE (SELF%NADMAP)
ENDIF

LNADCORE = ALLOCATED (SELF%NADCORE)
IF (LNADCORE) THEN
  !$ACC EXIT DATA DETACH (SELF%NADCORE)
  !$ACC EXIT DATA DELETE (SELF%NADCORE)
ENDIF

LLADCORE = ALLOCATED (SELF%LADCORE)
IF (LLADCORE) THEN
  !$ACC EXIT DATA DETACH (SELF%LADCORE)
  !$ACC EXIT DATA DELETE (SELF%LADCORE)
ENDIF

LRSASIGN = ALLOCATED (SELF%RSASIGN)
IF (LRSASIGN) THEN
  !$ACC EXIT DATA DETACH (SELF%RSASIGN)
  !$ACC EXIT DATA DELETE (SELF%RSASIGN)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

END SUBMODULE
