SUBMODULE (YEMLBC_GEO) YEMLBC_GEO_TELBC_GEO_COPY_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_COPY_TELBC_GEO (SELF, LDCREATED, LDFIELDAPI)

IMPLICIT NONE
CLASS (TELBC_GEO), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDCREATED, LDFIELDAPI
LOGICAL :: LLCREATED
LOGICAL :: LLFIELDAPI
LOGICAL :: LMPTRCPLBLK, LNIND_LEN, LNIND_LIST
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LLCREATED = .FALSE.
IF (PRESENT (LDCREATED)) THEN
  LLCREATED = LDCREATED
ENDIF
IF (.NOT. LLCREATED) THEN
  !$ACC ENTER DATA CREATE (SELF)
  !$ACC UPDATE DEVICE (SELF)
ENDIF
LNIND_LIST = ALLOCATED (SELF%NIND_LIST)
IF (LNIND_LIST) THEN
  !$ACC ENTER DATA CREATE (SELF%NIND_LIST)
  !$ACC UPDATE DEVICE (SELF%NIND_LIST)
  !$ACC ENTER DATA ATTACH (SELF%NIND_LIST)
ENDIF

LNIND_LEN = ALLOCATED (SELF%NIND_LEN)
IF (LNIND_LEN) THEN
  !$ACC ENTER DATA CREATE (SELF%NIND_LEN)
  !$ACC UPDATE DEVICE (SELF%NIND_LEN)
  !$ACC ENTER DATA ATTACH (SELF%NIND_LEN)
ENDIF

LMPTRCPLBLK = ALLOCATED (SELF%MPTRCPLBLK)
IF (LMPTRCPLBLK) THEN
  !$ACC ENTER DATA CREATE (SELF%MPTRCPLBLK)
  !$ACC UPDATE DEVICE (SELF%MPTRCPLBLK)
  !$ACC ENTER DATA ATTACH (SELF%MPTRCPLBLK)
ENDIF

END SUBROUTINE

END SUBMODULE
