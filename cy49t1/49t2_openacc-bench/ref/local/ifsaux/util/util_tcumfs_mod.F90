MODULE UTIL_TCUMFS_MOD

USE YOMCUMFS, ONLY : TCUMFS

INTERFACE SAVE
MODULE PROCEDURE SAVE_TCUMFS
END INTERFACE

INTERFACE LOAD
MODULE PROCEDURE LOAD_TCUMFS
END INTERFACE

INTERFACE COPY
MODULE PROCEDURE COPY_TCUMFS
END INTERFACE

INTERFACE HOST
MODULE PROCEDURE HOST_TCUMFS
END INTERFACE

     
INTERFACE CRC64
MODULE PROCEDURE CRC64_TCUMFS
END INTERFACE

INTERFACE WIPE
MODULE PROCEDURE WIPE_TCUMFS
END INTERFACE

INTERFACE SIZE
MODULE PROCEDURE SIZE_TCUMFS
END INTERFACE


CONTAINS

SUBROUTINE SAVE_TCUMFS (SELF, KLUN)

IMPLICIT NONE
TYPE (TCUMFS), INTENT (IN), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN

WRITE (KLUN) SELF%LECUMFS
WRITE (KLUN) SELF%LREGCV
WRITE (KLUN) SELF%LMFCFL2_SHSTEP
END SUBROUTINE

SUBROUTINE LOAD_TCUMFS (SELF, KLUN)

IMPLICIT NONE
TYPE (TCUMFS), INTENT (OUT), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN

READ (KLUN) SELF%LECUMFS
READ (KLUN) SELF%LREGCV
READ (KLUN) SELF%LMFCFL2_SHSTEP
END SUBROUTINE


SUBROUTINE COPY_TCUMFS (SELF, LDCREATED, LDFIELDAPI)

IMPLICIT NONE
TYPE (TCUMFS), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDCREATED, LDFIELDAPI
LOGICAL :: LLCREATED
LOGICAL :: LLFIELDAPI

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LLCREATED = .FALSE.
IF (PRESENT (LDCREATED)) THEN
  LLCREATED = LDCREATED
ENDIF
IF (.NOT. LLCREATED) THEN
  !$ACC ENTER DATA CREATE (SELF)
  !$ACC UPDATE DEVICE (SELF)
ENDIF

END SUBROUTINE

SUBROUTINE HOST_TCUMFS (SELF)

IMPLICIT NONE
TYPE (TCUMFS), TARGET :: SELF


END SUBROUTINE

     

SUBROUTINE CRC64_TCUMFS (SELF, KLUN, CDPATH)
USE CRC64_INTRINSIC, ONLY : FCRC64 => CRC64

IMPLICIT NONE
TYPE (TCUMFS), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN
CHARACTER(LEN=*), INTENT (IN) :: CDPATH
CHARACTER(LEN=128) :: CLIND
WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%LECUMFS), CDPATH//'%LECUMFS'

WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%LREGCV), CDPATH//'%LREGCV'

WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%LMFCFL2_SHSTEP), CDPATH//'%LMFCFL2_SHSTEP'

END SUBROUTINE

SUBROUTINE WIPE_TCUMFS (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
TYPE (TCUMFS), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

FUNCTION SIZE_TCUMFS (SELF, CDPATH, LDPRINT) RESULT (KSIZE)

IMPLICIT NONE
TYPE (TCUMFS),     INTENT (IN), TARGET :: SELF
CHARACTER(LEN=*), INTENT (IN), OPTIONAL :: CDPATH
LOGICAL,          INTENT (IN), OPTIONAL :: LDPRINT
INTEGER*8 :: KSIZE
INTEGER*8 :: ISIZE, JSIZE
LOGICAL :: LLPRINT
CHARACTER(LEN=128) :: CLPATH

LLPRINT = .FALSE.
IF (PRESENT (LDPRINT)) THEN
  LLPRINT = LDPRINT
ENDIF
CLPATH=''
IF (PRESENT (CDPATH)) THEN
  CLPATH = CDPATH
ENDIF
KSIZE = 0
ISIZE = KIND (SELF%LECUMFS)
IF (LLPRINT) THEN
  WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
  WRITE (*, *) TRIM (CLPATH)//'%LECUMFS'
ENDIF
KSIZE = KSIZE + ISIZE
ISIZE = KIND (SELF%LREGCV)
IF (LLPRINT) THEN
  WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
  WRITE (*, *) TRIM (CLPATH)//'%LREGCV'
ENDIF
KSIZE = KSIZE + ISIZE
ISIZE = KIND (SELF%LMFCFL2_SHSTEP)
IF (LLPRINT) THEN
  WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
  WRITE (*, *) TRIM (CLPATH)//'%LMFCFL2_SHSTEP'
ENDIF
KSIZE = KSIZE + ISIZE
END FUNCTION

END MODULE
