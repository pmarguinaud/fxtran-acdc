SUBMODULE (MF_PHYS_BASE_STATE_TYPE_MOD) MF_PHYS_BASE_STATE_TYPE_MOD_MF_PHYS_STATE_GSP_RR_TYPE_WIPE_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_WIPE_MF_PHYS_STATE_GSP_RR_TYPE (SELF, LDDELETED, LDFIELDAPI)
USE FIELD_UTIL_MODULE
IMPLICIT NONE
CLASS (MF_PHYS_STATE_GSP_RR_TYPE), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LF_FC, LF_IC, LF_T, LF_W
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LF_T = ASSOCIATED (SELF%F_T)
IF (LF_T .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_T)
ENDIF

LF_FC = ASSOCIATED (SELF%F_FC)
IF (LF_FC .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_FC)
ENDIF

LF_W = ASSOCIATED (SELF%F_W)
IF (LF_W .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_W)
ENDIF

LF_IC = ASSOCIATED (SELF%F_IC)
IF (LF_IC .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_IC)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

END SUBMODULE
