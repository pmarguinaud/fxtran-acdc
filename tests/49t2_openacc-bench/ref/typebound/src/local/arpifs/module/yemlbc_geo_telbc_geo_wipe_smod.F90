SUBMODULE (YEMLBC_GEO) YEMLBC_GEO_TELBC_GEO_WIPE_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_WIPE_TELBC_GEO (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
CLASS (TELBC_GEO), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LMPTRCPLBLK, LNIND_LEN, LNIND_LIST
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LNIND_LIST = ALLOCATED (SELF%NIND_LIST)
IF (LNIND_LIST) THEN
  !$ACC EXIT DATA DETACH (SELF%NIND_LIST)
  !$ACC EXIT DATA DELETE (SELF%NIND_LIST)
ENDIF

LNIND_LEN = ALLOCATED (SELF%NIND_LEN)
IF (LNIND_LEN) THEN
  !$ACC EXIT DATA DETACH (SELF%NIND_LEN)
  !$ACC EXIT DATA DELETE (SELF%NIND_LEN)
ENDIF

LMPTRCPLBLK = ALLOCATED (SELF%MPTRCPLBLK)
IF (LMPTRCPLBLK) THEN
  !$ACC EXIT DATA DETACH (SELF%MPTRCPLBLK)
  !$ACC EXIT DATA DELETE (SELF%MPTRCPLBLK)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

END SUBMODULE
