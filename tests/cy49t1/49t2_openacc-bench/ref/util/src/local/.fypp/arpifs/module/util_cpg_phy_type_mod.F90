MODULE UTIL_CPG_PHY_TYPE_MOD

USE CPG_TYPE_MOD, ONLY : CPG_PHY_TYPE



INTERFACE ACDC_COPY
MODULE PROCEDURE ACDC_COPY_CPG_PHY_TYPE
END INTERFACE

INTERFACE ACDC_HOST
MODULE PROCEDURE ACDC_HOST_CPG_PHY_TYPE
END INTERFACE



INTERFACE ACDC_WIPE
MODULE PROCEDURE ACDC_WIPE_CPG_PHY_TYPE
END INTERFACE


CONTAINS





SUBROUTINE ACDC_COPY_CPG_PHY_TYPE (SELF, LDCREATED, LDFIELDAPI)
USE FIELD_UTIL_MODULE
USE UTIL_CPG_XYB_TYPE_MOD
IMPLICIT NONE
TYPE (CPG_PHY_TYPE), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDCREATED, LDFIELDAPI
LOGICAL :: LLCREATED
LOGICAL :: LLFIELDAPI
LOGICAL :: LF_PRE, LF_PREF, LF_PREHYD, LF_PREHYDF, LF_W, LF_WL, LF_WM
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LLCREATED = .FALSE.
IF (PRESENT (LDCREATED)) THEN
  LLCREATED = LDCREATED
ENDIF
IF (.NOT. LLCREATED) THEN
  !$ACC ENTER DATA CREATE (SELF)
  !$ACC UPDATE DEVICE (SELF)
ENDIF
LF_PRE = ASSOCIATED (SELF%F_PRE)
IF (LF_PRE .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_3RB (SELF%F_PRE)
  !$ACC ENTER DATA ATTACH (SELF%F_PRE)
ENDIF

LF_PREF = ASSOCIATED (SELF%F_PREF)
IF (LF_PREF .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_3RB (SELF%F_PREF)
  !$ACC ENTER DATA ATTACH (SELF%F_PREF)
ENDIF

LF_PREHYD = ASSOCIATED (SELF%F_PREHYD)
IF (LF_PREHYD .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_3RB (SELF%F_PREHYD)
  !$ACC ENTER DATA ATTACH (SELF%F_PREHYD)
ENDIF

LF_PREHYDF = ASSOCIATED (SELF%F_PREHYDF)
IF (LF_PREHYDF .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_3RB (SELF%F_PREHYDF)
  !$ACC ENTER DATA ATTACH (SELF%F_PREHYDF)
ENDIF

LF_WL = ASSOCIATED (SELF%F_WL)
IF (LF_WL .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_3RB (SELF%F_WL)
  !$ACC ENTER DATA ATTACH (SELF%F_WL)
ENDIF

LF_WM = ASSOCIATED (SELF%F_WM)
IF (LF_WM .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_3RB (SELF%F_WM)
  !$ACC ENTER DATA ATTACH (SELF%F_WM)
ENDIF

LF_W = ASSOCIATED (SELF%F_W)
IF (LF_W .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_3RB (SELF%F_W)
  !$ACC ENTER DATA ATTACH (SELF%F_W)
ENDIF

CALL ACDC_COPY_CPG_XYB_TYPE (SELF%XYB, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)

END SUBROUTINE

SUBROUTINE ACDC_HOST_CPG_PHY_TYPE (SELF)
USE FIELD_UTIL_MODULE
USE UTIL_CPG_XYB_TYPE_MOD
IMPLICIT NONE
TYPE (CPG_PHY_TYPE), TARGET :: SELF
LOGICAL :: LF_PRE, LF_PREF, LF_PREHYD, LF_PREHYDF, LF_W, LF_WL, LF_WM
LF_PRE = ASSOCIATED (SELF%F_PRE)
IF (LF_PRE) THEN
  CALL HOST_FIELD_3RB (SELF%F_PRE)
ENDIF

LF_PREF = ASSOCIATED (SELF%F_PREF)
IF (LF_PREF) THEN
  CALL HOST_FIELD_3RB (SELF%F_PREF)
ENDIF

LF_PREHYD = ASSOCIATED (SELF%F_PREHYD)
IF (LF_PREHYD) THEN
  CALL HOST_FIELD_3RB (SELF%F_PREHYD)
ENDIF

LF_PREHYDF = ASSOCIATED (SELF%F_PREHYDF)
IF (LF_PREHYDF) THEN
  CALL HOST_FIELD_3RB (SELF%F_PREHYDF)
ENDIF

LF_WL = ASSOCIATED (SELF%F_WL)
IF (LF_WL) THEN
  CALL HOST_FIELD_3RB (SELF%F_WL)
ENDIF

LF_WM = ASSOCIATED (SELF%F_WM)
IF (LF_WM) THEN
  CALL HOST_FIELD_3RB (SELF%F_WM)
ENDIF

LF_W = ASSOCIATED (SELF%F_W)
IF (LF_W) THEN
  CALL HOST_FIELD_3RB (SELF%F_W)
ENDIF

CALL ACDC_HOST_CPG_XYB_TYPE (SELF%XYB)

END SUBROUTINE





SUBROUTINE ACDC_WIPE_CPG_PHY_TYPE (SELF, LDDELETED, LDFIELDAPI)
USE FIELD_UTIL_MODULE
USE UTIL_CPG_XYB_TYPE_MOD
IMPLICIT NONE
TYPE (CPG_PHY_TYPE), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LF_PRE, LF_PREF, LF_PREHYD, LF_PREHYDF, LF_W, LF_WL, LF_WM
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LF_PRE = ASSOCIATED (SELF%F_PRE)
IF (LF_PRE .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_PRE)
ENDIF

LF_PREF = ASSOCIATED (SELF%F_PREF)
IF (LF_PREF .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_PREF)
ENDIF

LF_PREHYD = ASSOCIATED (SELF%F_PREHYD)
IF (LF_PREHYD .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_PREHYD)
ENDIF

LF_PREHYDF = ASSOCIATED (SELF%F_PREHYDF)
IF (LF_PREHYDF .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_PREHYDF)
ENDIF

LF_WL = ASSOCIATED (SELF%F_WL)
IF (LF_WL .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_WL)
ENDIF

LF_WM = ASSOCIATED (SELF%F_WM)
IF (LF_WM .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_WM)
ENDIF

LF_W = ASSOCIATED (SELF%F_W)
IF (LF_W .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_W)
ENDIF

CALL ACDC_WIPE_CPG_XYB_TYPE (SELF%XYB, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE



END MODULE
