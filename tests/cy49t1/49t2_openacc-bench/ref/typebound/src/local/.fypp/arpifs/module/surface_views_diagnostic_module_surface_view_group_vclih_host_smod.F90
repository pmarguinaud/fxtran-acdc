SUBMODULE (SURFACE_VIEWS_DIAGNOSTIC_MODULE) SURFACE_VIEWS_DIAGNOSTIC_MODULE_SURFACE_VIEW_GROUP_VCLIH_HOST_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_HOST_SURFACE_VIEW_GROUP_VCLIH (SELF)
USE FIELD_UTIL_MODULE

IMPLICIT NONE
CLASS (SURFACE_VIEW_GROUP_VCLIH), TARGET :: SELF
LOGICAL :: LF_BCCH, LF_EVA, LF_GROUP, LF_PBLH, LF_PCL, LF_PCN, LF_PSL, LF_PSN, LF_QSH, LF_SCCH
LOGICAL :: LF_SPSH, LF_TCCH, LVARIABLE_GROUP
LVARIABLE_GROUP = ASSOCIATED (SELF%VARIABLE_GROUP)
IF (LVARIABLE_GROUP) THEN
  CALL SELF%VARIABLE_GROUP%ACDC_HOST ()
ENDIF

LF_TCCH = ASSOCIATED (SELF%F_TCCH)
IF (LF_TCCH) THEN
  CALL HOST_FIELD_2RB (SELF%F_TCCH)
ENDIF

LF_SCCH = ASSOCIATED (SELF%F_SCCH)
IF (LF_SCCH) THEN
  CALL HOST_FIELD_2RB (SELF%F_SCCH)
ENDIF

LF_BCCH = ASSOCIATED (SELF%F_BCCH)
IF (LF_BCCH) THEN
  CALL HOST_FIELD_2RB (SELF%F_BCCH)
ENDIF

LF_PBLH = ASSOCIATED (SELF%F_PBLH)
IF (LF_PBLH) THEN
  CALL HOST_FIELD_2RB (SELF%F_PBLH)
ENDIF

LF_SPSH = ASSOCIATED (SELF%F_SPSH)
IF (LF_SPSH) THEN
  CALL HOST_FIELD_2RB (SELF%F_SPSH)
ENDIF

LF_QSH = ASSOCIATED (SELF%F_QSH)
IF (LF_QSH) THEN
  CALL HOST_FIELD_2RB (SELF%F_QSH)
ENDIF

LF_PCL = ASSOCIATED (SELF%F_PCL)
IF (LF_PCL) THEN
  CALL HOST_FIELD_2RB (SELF%F_PCL)
ENDIF

LF_PSL = ASSOCIATED (SELF%F_PSL)
IF (LF_PSL) THEN
  CALL HOST_FIELD_2RB (SELF%F_PSL)
ENDIF

LF_PCN = ASSOCIATED (SELF%F_PCN)
IF (LF_PCN) THEN
  CALL HOST_FIELD_2RB (SELF%F_PCN)
ENDIF

LF_PSN = ASSOCIATED (SELF%F_PSN)
IF (LF_PSN) THEN
  CALL HOST_FIELD_2RB (SELF%F_PSN)
ENDIF

LF_EVA = ASSOCIATED (SELF%F_EVA)
IF (LF_EVA) THEN
  CALL HOST_FIELD_2RB (SELF%F_EVA)
ENDIF

LF_GROUP = ASSOCIATED (SELF%F_GROUP)
IF (LF_GROUP) THEN
  CALL HOST_FIELD_3RB (SELF%F_GROUP)
ENDIF

END SUBROUTINE

END SUBMODULE
