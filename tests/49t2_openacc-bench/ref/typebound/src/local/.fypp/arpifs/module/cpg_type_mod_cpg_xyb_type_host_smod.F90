SUBMODULE (CPG_TYPE_MOD) CPG_TYPE_MOD_CPG_XYB_TYPE_HOST_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_HOST_CPG_XYB_TYPE (SELF)
USE FIELD_UTIL_MODULE
IMPLICIT NONE
CLASS (CPG_XYB_TYPE), TARGET :: SELF
LOGICAL :: LF_ALPH, LF_DELP, LF_LNPR, LF_RDELP, LF_RPP, LF_RPRE, LF_RTGR

LF_DELP = ASSOCIATED (SELF%F_DELP)
IF (LF_DELP) THEN
  CALL HOST_FIELD_3RB (SELF%F_DELP)
ENDIF

LF_RDELP = ASSOCIATED (SELF%F_RDELP)
IF (LF_RDELP) THEN
  CALL HOST_FIELD_3RB (SELF%F_RDELP)
ENDIF

LF_LNPR = ASSOCIATED (SELF%F_LNPR)
IF (LF_LNPR) THEN
  CALL HOST_FIELD_3RB (SELF%F_LNPR)
ENDIF

LF_ALPH = ASSOCIATED (SELF%F_ALPH)
IF (LF_ALPH) THEN
  CALL HOST_FIELD_3RB (SELF%F_ALPH)
ENDIF

LF_RTGR = ASSOCIATED (SELF%F_RTGR)
IF (LF_RTGR) THEN
  CALL HOST_FIELD_3RB (SELF%F_RTGR)
ENDIF

LF_RPRE = ASSOCIATED (SELF%F_RPRE)
IF (LF_RPRE) THEN
  CALL HOST_FIELD_3RB (SELF%F_RPRE)
ENDIF

LF_RPP = ASSOCIATED (SELF%F_RPP)
IF (LF_RPP) THEN
  CALL HOST_FIELD_3RB (SELF%F_RPP)
ENDIF

END SUBROUTINE

END SUBMODULE
