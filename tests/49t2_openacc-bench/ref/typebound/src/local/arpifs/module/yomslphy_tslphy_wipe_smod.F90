SUBMODULE (YOMSLPHY) YOMSLPHY_TSLPHY_WIPE_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_WIPE_TSLPHY (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
CLASS (TSLPHY), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LRSLWX, LSAVTEND, LSAVTEND5
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LRSLWX = ALLOCATED (SELF%RSLWX)
IF (LRSLWX) THEN
  !$ACC EXIT DATA DETACH (SELF%RSLWX)
  !$ACC EXIT DATA DELETE (SELF%RSLWX)
ENDIF

LSAVTEND = ALLOCATED (SELF%SAVTEND)
IF (LSAVTEND) THEN
  !$ACC EXIT DATA DETACH (SELF%SAVTEND)
  !$ACC EXIT DATA DELETE (SELF%SAVTEND)
ENDIF

LSAVTEND5 = ALLOCATED (SELF%SAVTEND5)
IF (LSAVTEND5) THEN
  !$ACC EXIT DATA DETACH (SELF%SAVTEND5)
  !$ACC EXIT DATA DELETE (SELF%SAVTEND5)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

END SUBMODULE
