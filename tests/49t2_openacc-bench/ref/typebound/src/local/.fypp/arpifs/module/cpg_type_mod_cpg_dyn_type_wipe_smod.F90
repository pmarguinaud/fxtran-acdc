SUBMODULE (CPG_TYPE_MOD) CPG_TYPE_MOD_CPG_DYN_TYPE_WIPE_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_WIPE_CPG_DYN_TYPE (SELF, LDDELETED, LDFIELDAPI)
USE FIELD_UTIL_MODULE
IMPLICIT NONE
CLASS (CPG_DYN_TYPE), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LF_DBBC, LF_GWFL, LF_GWFM, LF_GWFT, LF_GWS, LF_GWT, LF_KENE, LF_NHPREF, LF_NHPREH, LF_NHX
LOGICAL :: LF_NHY, LF_OROGL, LF_OROGLL, LF_OROGLM, LF_OROGM, LF_OROGMM, LF_PHI, LF_PHIF, LF_PHIFL, LF_PHIFM
LOGICAL :: LF_PRE, LF_PREF, LF_PREL, LF_PREM, LF_QCHAL, LF_QCHAM, LF_RDPHI, LF_RTL, LF_RTM
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LF_OROGL = ASSOCIATED (SELF%F_OROGL)
IF (LF_OROGL .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_OROGL)
ENDIF

LF_OROGM = ASSOCIATED (SELF%F_OROGM)
IF (LF_OROGM .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_OROGM)
ENDIF

LF_OROGLL = ASSOCIATED (SELF%F_OROGLL)
IF (LF_OROGLL .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_OROGLL)
ENDIF

LF_OROGLM = ASSOCIATED (SELF%F_OROGLM)
IF (LF_OROGLM .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_OROGLM)
ENDIF

LF_OROGMM = ASSOCIATED (SELF%F_OROGMM)
IF (LF_OROGMM .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_OROGMM)
ENDIF

LF_PHI = ASSOCIATED (SELF%F_PHI)
IF (LF_PHI .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_PHI)
ENDIF

LF_PRE = ASSOCIATED (SELF%F_PRE)
IF (LF_PRE .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_PRE)
ENDIF

LF_PHIF = ASSOCIATED (SELF%F_PHIF)
IF (LF_PHIF .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_PHIF)
ENDIF

LF_PREF = ASSOCIATED (SELF%F_PREF)
IF (LF_PREF .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_PREF)
ENDIF

LF_PREL = ASSOCIATED (SELF%F_PREL)
IF (LF_PREL .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_PREL)
ENDIF

LF_PREM = ASSOCIATED (SELF%F_PREM)
IF (LF_PREM .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_PREM)
ENDIF

LF_NHY = ASSOCIATED (SELF%F_NHY)
IF (LF_NHY .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_NHY)
ENDIF

LF_GWFT = ASSOCIATED (SELF%F_GWFT)
IF (LF_GWFT .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_GWFT)
ENDIF

LF_KENE = ASSOCIATED (SELF%F_KENE)
IF (LF_KENE .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_KENE)
ENDIF

LF_NHX = ASSOCIATED (SELF%F_NHX)
IF (LF_NHX .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_NHX)
ENDIF

LF_PHIFL = ASSOCIATED (SELF%F_PHIFL)
IF (LF_PHIFL .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_PHIFL)
ENDIF

LF_PHIFM = ASSOCIATED (SELF%F_PHIFM)
IF (LF_PHIFM .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_PHIFM)
ENDIF

LF_RTL = ASSOCIATED (SELF%F_RTL)
IF (LF_RTL .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_RTL)
ENDIF

LF_RTM = ASSOCIATED (SELF%F_RTM)
IF (LF_RTM .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_RTM)
ENDIF

CALL SELF%RCP%ACDC_WIPE (LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)

CALL SELF%CTY%ACDC_WIPE (LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)

CALL SELF%UVH%ACDC_WIPE (LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)

CALL SELF%XYB%ACDC_WIPE (LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)

LF_DBBC = ASSOCIATED (SELF%F_DBBC)
IF (LF_DBBC .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_DBBC)
ENDIF

LF_GWS = ASSOCIATED (SELF%F_GWS)
IF (LF_GWS .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_GWS)
ENDIF

LF_GWFL = ASSOCIATED (SELF%F_GWFL)
IF (LF_GWFL .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_GWFL)
ENDIF

LF_GWFM = ASSOCIATED (SELF%F_GWFM)
IF (LF_GWFM .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_GWFM)
ENDIF

LF_GWT = ASSOCIATED (SELF%F_GWT)
IF (LF_GWT .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_GWT)
ENDIF

LF_NHPREF = ASSOCIATED (SELF%F_NHPREF)
IF (LF_NHPREF .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_NHPREF)
ENDIF

LF_NHPREH = ASSOCIATED (SELF%F_NHPREH)
IF (LF_NHPREH .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_NHPREH)
ENDIF

LF_QCHAL = ASSOCIATED (SELF%F_QCHAL)
IF (LF_QCHAL .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_QCHAL)
ENDIF

LF_QCHAM = ASSOCIATED (SELF%F_QCHAM)
IF (LF_QCHAM .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_QCHAM)
ENDIF

LF_RDPHI = ASSOCIATED (SELF%F_RDPHI)
IF (LF_RDPHI .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_RDPHI)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

END SUBMODULE
