SUBMODULE (YOEAERC) YOEAERC_TEAERC_TEGEN_COPY_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_COPY_TEAERC_TEGEN (SELF, LDCREATED, LDFIELDAPI)

IMPLICIT NONE
CLASS (TEAERC_TEGEN), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDCREATED, LDFIELDAPI
LOGICAL :: LLCREATED
LOGICAL :: LLFIELDAPI
LOGICAL :: LRAERBC, LRAEROR, LRAERSD, LRAERSS, LRAERSU, LRAERV, LRLATV
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LLCREATED = .FALSE.
IF (PRESENT (LDCREATED)) THEN
  LLCREATED = LDCREATED
ENDIF
IF (.NOT. LLCREATED) THEN
  !$ACC ENTER DATA CREATE (SELF)
  !$ACC UPDATE DEVICE (SELF)
ENDIF

LRAERBC = ASSOCIATED (SELF%RAERBC)
IF (LRAERBC) THEN
  !$ACC ENTER DATA CREATE (SELF%RAERBC)
  !$ACC UPDATE DEVICE (SELF%RAERBC)
  !$ACC ENTER DATA ATTACH (SELF%RAERBC)
ENDIF

LRAEROR = ASSOCIATED (SELF%RAEROR)
IF (LRAEROR) THEN
  !$ACC ENTER DATA CREATE (SELF%RAEROR)
  !$ACC UPDATE DEVICE (SELF%RAEROR)
  !$ACC ENTER DATA ATTACH (SELF%RAEROR)
ENDIF

LRAERSD = ASSOCIATED (SELF%RAERSD)
IF (LRAERSD) THEN
  !$ACC ENTER DATA CREATE (SELF%RAERSD)
  !$ACC UPDATE DEVICE (SELF%RAERSD)
  !$ACC ENTER DATA ATTACH (SELF%RAERSD)
ENDIF

LRAERSS = ASSOCIATED (SELF%RAERSS)
IF (LRAERSS) THEN
  !$ACC ENTER DATA CREATE (SELF%RAERSS)
  !$ACC UPDATE DEVICE (SELF%RAERSS)
  !$ACC ENTER DATA ATTACH (SELF%RAERSS)
ENDIF

LRAERSU = ASSOCIATED (SELF%RAERSU)
IF (LRAERSU) THEN
  !$ACC ENTER DATA CREATE (SELF%RAERSU)
  !$ACC UPDATE DEVICE (SELF%RAERSU)
  !$ACC ENTER DATA ATTACH (SELF%RAERSU)
ENDIF

LRAERV = ASSOCIATED (SELF%RAERV)
IF (LRAERV) THEN
  !$ACC ENTER DATA CREATE (SELF%RAERV)
  !$ACC UPDATE DEVICE (SELF%RAERV)
  !$ACC ENTER DATA ATTACH (SELF%RAERV)
ENDIF

LRLATV = ASSOCIATED (SELF%RLATV)
IF (LRLATV) THEN
  !$ACC ENTER DATA CREATE (SELF%RLATV)
  !$ACC UPDATE DEVICE (SELF%RLATV)
  !$ACC ENTER DATA ATTACH (SELF%RLATV)
ENDIF

END SUBROUTINE

END SUBMODULE
