SUBMODULE (YOMCDDH) YOMCDDH_TCDDH_WIPE_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_WIPE_TCDDH (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
CLASS (TCDDH), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LCADHFSS, LCADHIIS, LCADHSSS, LCADHTIS, LCADHTLS, LCADHTSS, LCADHTTS, LCADHWLS
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LCADHTLS = ALLOCATED (SELF%CADHTLS)
IF (LCADHTLS) THEN
  !$ACC EXIT DATA DETACH (SELF%CADHTLS)
  !$ACC EXIT DATA DELETE (SELF%CADHTLS)
ENDIF

LCADHTSS = ALLOCATED (SELF%CADHTSS)
IF (LCADHTSS) THEN
  !$ACC EXIT DATA DETACH (SELF%CADHTSS)
  !$ACC EXIT DATA DELETE (SELF%CADHTSS)
ENDIF

LCADHTTS = ALLOCATED (SELF%CADHTTS)
IF (LCADHTTS) THEN
  !$ACC EXIT DATA DETACH (SELF%CADHTTS)
  !$ACC EXIT DATA DELETE (SELF%CADHTTS)
ENDIF

LCADHTIS = ALLOCATED (SELF%CADHTIS)
IF (LCADHTIS) THEN
  !$ACC EXIT DATA DETACH (SELF%CADHTIS)
  !$ACC EXIT DATA DELETE (SELF%CADHTIS)
ENDIF

LCADHSSS = ALLOCATED (SELF%CADHSSS)
IF (LCADHSSS) THEN
  !$ACC EXIT DATA DETACH (SELF%CADHSSS)
  !$ACC EXIT DATA DELETE (SELF%CADHSSS)
ENDIF

LCADHIIS = ALLOCATED (SELF%CADHIIS)
IF (LCADHIIS) THEN
  !$ACC EXIT DATA DETACH (SELF%CADHIIS)
  !$ACC EXIT DATA DELETE (SELF%CADHIIS)
ENDIF

LCADHWLS = ALLOCATED (SELF%CADHWLS)
IF (LCADHWLS) THEN
  !$ACC EXIT DATA DETACH (SELF%CADHWLS)
  !$ACC EXIT DATA DELETE (SELF%CADHWLS)
ENDIF

LCADHFSS = ALLOCATED (SELF%CADHFSS)
IF (LCADHFSS) THEN
  !$ACC EXIT DATA DETACH (SELF%CADHFSS)
  !$ACC EXIT DATA DELETE (SELF%CADHFSS)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

END SUBMODULE
