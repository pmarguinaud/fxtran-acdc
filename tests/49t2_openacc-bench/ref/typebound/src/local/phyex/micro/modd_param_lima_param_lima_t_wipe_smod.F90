SUBMODULE (MODD_PARAM_LIMA) MODD_PARAM_LIMA_PARAM_LIMA_T_WIPE_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_WIPE_PARAM_LIMA_T (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
CLASS (PARAM_LIMA_T), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LNIMM, LNINDICE_CCN_IMM, LXABSCISS, LXBETAHEN_MULTI, LXCONC_CCN_TOT, LXCTMIN, LXFRAC, LXFRAC_REF, LXKHEN_MULTI, LXLIMIT_FACTOR
LOGICAL :: LXLOGSIG_CCN, LXMDIAM_IFN, LXMUHEN_MULTI, LXRHO_CCN, LXRHO_IFN, LXRTMIN, LXR_MEAN_CCN, LXSIGMA_IFN, LXWEIGHT
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF

LNIMM = ALLOCATED (SELF%NIMM)
IF (LNIMM) THEN
  !$ACC EXIT DATA DETACH (SELF%NIMM)
  !$ACC EXIT DATA DELETE (SELF%NIMM)
ENDIF

LNINDICE_CCN_IMM = ALLOCATED (SELF%NINDICE_CCN_IMM)
IF (LNINDICE_CCN_IMM) THEN
  !$ACC EXIT DATA DETACH (SELF%NINDICE_CCN_IMM)
  !$ACC EXIT DATA DELETE (SELF%NINDICE_CCN_IMM)
ENDIF

LXMDIAM_IFN = ALLOCATED (SELF%XMDIAM_IFN)
IF (LXMDIAM_IFN) THEN
  !$ACC EXIT DATA DETACH (SELF%XMDIAM_IFN)
  !$ACC EXIT DATA DELETE (SELF%XMDIAM_IFN)
ENDIF

LXSIGMA_IFN = ALLOCATED (SELF%XSIGMA_IFN)
IF (LXSIGMA_IFN) THEN
  !$ACC EXIT DATA DETACH (SELF%XSIGMA_IFN)
  !$ACC EXIT DATA DELETE (SELF%XSIGMA_IFN)
ENDIF

LXRHO_IFN = ALLOCATED (SELF%XRHO_IFN)
IF (LXRHO_IFN) THEN
  !$ACC EXIT DATA DETACH (SELF%XRHO_IFN)
  !$ACC EXIT DATA DELETE (SELF%XRHO_IFN)
ENDIF

LXFRAC = ALLOCATED (SELF%XFRAC)
IF (LXFRAC) THEN
  !$ACC EXIT DATA DETACH (SELF%XFRAC)
  !$ACC EXIT DATA DELETE (SELF%XFRAC)
ENDIF

LXFRAC_REF = ALLOCATED (SELF%XFRAC_REF)
IF (LXFRAC_REF) THEN
  !$ACC EXIT DATA DETACH (SELF%XFRAC_REF)
  !$ACC EXIT DATA DELETE (SELF%XFRAC_REF)
ENDIF

LXABSCISS = ALLOCATED (SELF%XABSCISS)
IF (LXABSCISS) THEN
  !$ACC EXIT DATA DETACH (SELF%XABSCISS)
  !$ACC EXIT DATA DELETE (SELF%XABSCISS)
ENDIF

LXWEIGHT = ALLOCATED (SELF%XWEIGHT)
IF (LXWEIGHT) THEN
  !$ACC EXIT DATA DETACH (SELF%XWEIGHT)
  !$ACC EXIT DATA DELETE (SELF%XWEIGHT)
ENDIF

LXR_MEAN_CCN = ALLOCATED (SELF%XR_MEAN_CCN)
IF (LXR_MEAN_CCN) THEN
  !$ACC EXIT DATA DETACH (SELF%XR_MEAN_CCN)
  !$ACC EXIT DATA DELETE (SELF%XR_MEAN_CCN)
ENDIF

LXLOGSIG_CCN = ALLOCATED (SELF%XLOGSIG_CCN)
IF (LXLOGSIG_CCN) THEN
  !$ACC EXIT DATA DETACH (SELF%XLOGSIG_CCN)
  !$ACC EXIT DATA DELETE (SELF%XLOGSIG_CCN)
ENDIF

LXRHO_CCN = ALLOCATED (SELF%XRHO_CCN)
IF (LXRHO_CCN) THEN
  !$ACC EXIT DATA DETACH (SELF%XRHO_CCN)
  !$ACC EXIT DATA DELETE (SELF%XRHO_CCN)
ENDIF

LXKHEN_MULTI = ALLOCATED (SELF%XKHEN_MULTI)
IF (LXKHEN_MULTI) THEN
  !$ACC EXIT DATA DETACH (SELF%XKHEN_MULTI)
  !$ACC EXIT DATA DELETE (SELF%XKHEN_MULTI)
ENDIF

LXMUHEN_MULTI = ALLOCATED (SELF%XMUHEN_MULTI)
IF (LXMUHEN_MULTI) THEN
  !$ACC EXIT DATA DETACH (SELF%XMUHEN_MULTI)
  !$ACC EXIT DATA DELETE (SELF%XMUHEN_MULTI)
ENDIF

LXBETAHEN_MULTI = ALLOCATED (SELF%XBETAHEN_MULTI)
IF (LXBETAHEN_MULTI) THEN
  !$ACC EXIT DATA DETACH (SELF%XBETAHEN_MULTI)
  !$ACC EXIT DATA DELETE (SELF%XBETAHEN_MULTI)
ENDIF

LXCONC_CCN_TOT = ALLOCATED (SELF%XCONC_CCN_TOT)
IF (LXCONC_CCN_TOT) THEN
  !$ACC EXIT DATA DETACH (SELF%XCONC_CCN_TOT)
  !$ACC EXIT DATA DELETE (SELF%XCONC_CCN_TOT)
ENDIF

LXLIMIT_FACTOR = ALLOCATED (SELF%XLIMIT_FACTOR)
IF (LXLIMIT_FACTOR) THEN
  !$ACC EXIT DATA DETACH (SELF%XLIMIT_FACTOR)
  !$ACC EXIT DATA DELETE (SELF%XLIMIT_FACTOR)
ENDIF

LXRTMIN = ALLOCATED (SELF%XRTMIN)
IF (LXRTMIN) THEN
  !$ACC EXIT DATA DETACH (SELF%XRTMIN)
  !$ACC EXIT DATA DELETE (SELF%XRTMIN)
ENDIF

LXCTMIN = ALLOCATED (SELF%XCTMIN)
IF (LXCTMIN) THEN
  !$ACC EXIT DATA DETACH (SELF%XCTMIN)
  !$ACC EXIT DATA DELETE (SELF%XCTMIN)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

END SUBMODULE
