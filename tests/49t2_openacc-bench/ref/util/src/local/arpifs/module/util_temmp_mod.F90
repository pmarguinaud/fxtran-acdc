MODULE UTIL_TEMMP_MOD

USE YEMMP, ONLY : TEMMP

INTERFACE ACDC_SAVE
MODULE PROCEDURE ACDC_SAVE_TEMMP
END INTERFACE

INTERFACE ACDC_LOAD
MODULE PROCEDURE ACDC_LOAD_TEMMP
END INTERFACE

INTERFACE ACDC_COPY
MODULE PROCEDURE ACDC_COPY_TEMMP
END INTERFACE

INTERFACE ACDC_HOST
MODULE PROCEDURE ACDC_HOST_TEMMP
END INTERFACE


INTERFACE ACDC_CRC64
MODULE PROCEDURE ACDC_CRC64_TEMMP
END INTERFACE

INTERFACE ACDC_WIPE
MODULE PROCEDURE ACDC_WIPE_TEMMP
END INTERFACE

INTERFACE ACDC_SIZE
MODULE PROCEDURE ACDC_SIZE_TEMMP
END INTERFACE

CONTAINS

SUBROUTINE ACDC_SAVE_TEMMP (SELF, KLUN)

IMPLICIT NONE
TYPE (TEMMP), INTENT (IN), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN
LOGICAL :: LMYENS, LNEALLNS, LNEPROCN, LNEPTRNS, LNUEMPP
LNEPROCN = ASSOCIATED (SELF%NEPROCN)
WRITE (KLUN) LNEPROCN
IF (LNEPROCN) THEN
  WRITE (KLUN) LBOUND (SELF%NEPROCN)
  WRITE (KLUN) UBOUND (SELF%NEPROCN)
  WRITE (KLUN) SELF%NEPROCN
ENDIF
WRITE (KLUN) SELF%NUEMP
LMYENS = ASSOCIATED (SELF%MYENS)
WRITE (KLUN) LMYENS
IF (LMYENS) THEN
  WRITE (KLUN) LBOUND (SELF%MYENS)
  WRITE (KLUN) UBOUND (SELF%MYENS)
  WRITE (KLUN) SELF%MYENS
ENDIF
LNUEMPP = ASSOCIATED (SELF%NUEMPP)
WRITE (KLUN) LNUEMPP
IF (LNUEMPP) THEN
  WRITE (KLUN) LBOUND (SELF%NUEMPP)
  WRITE (KLUN) UBOUND (SELF%NUEMPP)
  WRITE (KLUN) SELF%NUEMPP
ENDIF
LNEALLNS = ASSOCIATED (SELF%NEALLNS)
WRITE (KLUN) LNEALLNS
IF (LNEALLNS) THEN
  WRITE (KLUN) LBOUND (SELF%NEALLNS)
  WRITE (KLUN) UBOUND (SELF%NEALLNS)
  WRITE (KLUN) SELF%NEALLNS
ENDIF
LNEPTRNS = ASSOCIATED (SELF%NEPTRNS)
WRITE (KLUN) LNEPTRNS
IF (LNEPTRNS) THEN
  WRITE (KLUN) LBOUND (SELF%NEPTRNS)
  WRITE (KLUN) UBOUND (SELF%NEPTRNS)
  WRITE (KLUN) SELF%NEPTRNS
ENDIF
END SUBROUTINE

SUBROUTINE ACDC_LOAD_TEMMP (SELF, KLUN)

IMPLICIT NONE
TYPE (TEMMP), INTENT (OUT), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN
INTEGER :: IL1(1), IU1(1)
LOGICAL :: LMYENS, LNEALLNS, LNEPROCN, LNEPTRNS, LNUEMPP
READ (KLUN) LNEPROCN
IF (LNEPROCN) THEN
  READ (KLUN) IL1
  READ (KLUN) IU1
  ALLOCATE (SELF%NEPROCN (IL1(1):IU1(1)))
  READ (KLUN) SELF%NEPROCN
ELSE
  NULLIFY (SELF%NEPROCN)
ENDIF
READ (KLUN) SELF%NUEMP
READ (KLUN) LMYENS
IF (LMYENS) THEN
  READ (KLUN) IL1
  READ (KLUN) IU1
  ALLOCATE (SELF%MYENS (IL1(1):IU1(1)))
  READ (KLUN) SELF%MYENS
ELSE
  NULLIFY (SELF%MYENS)
ENDIF
READ (KLUN) LNUEMPP
IF (LNUEMPP) THEN
  READ (KLUN) IL1
  READ (KLUN) IU1
  ALLOCATE (SELF%NUEMPP (IL1(1):IU1(1)))
  READ (KLUN) SELF%NUEMPP
ELSE
  NULLIFY (SELF%NUEMPP)
ENDIF
READ (KLUN) LNEALLNS
IF (LNEALLNS) THEN
  READ (KLUN) IL1
  READ (KLUN) IU1
  ALLOCATE (SELF%NEALLNS (IL1(1):IU1(1)))
  READ (KLUN) SELF%NEALLNS
ELSE
  NULLIFY (SELF%NEALLNS)
ENDIF
READ (KLUN) LNEPTRNS
IF (LNEPTRNS) THEN
  READ (KLUN) IL1
  READ (KLUN) IU1
  ALLOCATE (SELF%NEPTRNS (IL1(1):IU1(1)))
  READ (KLUN) SELF%NEPTRNS
ELSE
  NULLIFY (SELF%NEPTRNS)
ENDIF
END SUBROUTINE

SUBROUTINE ACDC_COPY_TEMMP (SELF, LDCREATED, LDFIELDAPI)

IMPLICIT NONE
TYPE (TEMMP), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDCREATED, LDFIELDAPI
LOGICAL :: LLCREATED
LOGICAL :: LLFIELDAPI
LOGICAL :: LMYENS, LNEALLNS, LNEPROCN, LNEPTRNS, LNUEMPP
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LLCREATED = .FALSE.
IF (PRESENT (LDCREATED)) THEN
  LLCREATED = LDCREATED
ENDIF
IF (.NOT. LLCREATED) THEN
  !$ACC ENTER DATA CREATE (SELF)
  !$ACC UPDATE DEVICE (SELF)
ENDIF
LNEPROCN = ASSOCIATED (SELF%NEPROCN)
IF (LNEPROCN) THEN
  !$ACC ENTER DATA CREATE (SELF%NEPROCN)
  !$ACC UPDATE DEVICE (SELF%NEPROCN)
  !$ACC ENTER DATA ATTACH (SELF%NEPROCN)
ENDIF

LMYENS = ASSOCIATED (SELF%MYENS)
IF (LMYENS) THEN
  !$ACC ENTER DATA CREATE (SELF%MYENS)
  !$ACC UPDATE DEVICE (SELF%MYENS)
  !$ACC ENTER DATA ATTACH (SELF%MYENS)
ENDIF

LNUEMPP = ASSOCIATED (SELF%NUEMPP)
IF (LNUEMPP) THEN
  !$ACC ENTER DATA CREATE (SELF%NUEMPP)
  !$ACC UPDATE DEVICE (SELF%NUEMPP)
  !$ACC ENTER DATA ATTACH (SELF%NUEMPP)
ENDIF

LNEALLNS = ASSOCIATED (SELF%NEALLNS)
IF (LNEALLNS) THEN
  !$ACC ENTER DATA CREATE (SELF%NEALLNS)
  !$ACC UPDATE DEVICE (SELF%NEALLNS)
  !$ACC ENTER DATA ATTACH (SELF%NEALLNS)
ENDIF

LNEPTRNS = ASSOCIATED (SELF%NEPTRNS)
IF (LNEPTRNS) THEN
  !$ACC ENTER DATA CREATE (SELF%NEPTRNS)
  !$ACC UPDATE DEVICE (SELF%NEPTRNS)
  !$ACC ENTER DATA ATTACH (SELF%NEPTRNS)
ENDIF

END SUBROUTINE

SUBROUTINE ACDC_HOST_TEMMP (SELF)

IMPLICIT NONE
TYPE (TEMMP), TARGET :: SELF
LOGICAL :: LMYENS, LNEALLNS, LNEPROCN, LNEPTRNS, LNUEMPP

END SUBROUTINE



SUBROUTINE ACDC_CRC64_TEMMP (SELF, KLUN, CDPATH)
USE FXTRAN_ACDC_CRC64_INTRINSIC_MOD, ONLY : FCRC64 => FXTRAN_ACDC_CRC64
IMPLICIT NONE
TYPE (TEMMP), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN
CHARACTER(LEN=*), INTENT (IN) :: CDPATH
CHARACTER(LEN=128) :: CLIND
LOGICAL :: LMYENS, LNEALLNS, LNEPROCN, LNEPTRNS, LNUEMPP
WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%NEPROCN), CDPATH//'%NEPROCN'

WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%NUEMP), CDPATH//'%NUEMP'

WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%MYENS), CDPATH//'%MYENS'

WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%NUEMPP), CDPATH//'%NUEMPP'

WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%NEALLNS), CDPATH//'%NEALLNS'

WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%NEPTRNS), CDPATH//'%NEPTRNS'

END SUBROUTINE

SUBROUTINE ACDC_WIPE_TEMMP (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
TYPE (TEMMP), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LMYENS, LNEALLNS, LNEPROCN, LNEPTRNS, LNUEMPP
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LNEPROCN = ASSOCIATED (SELF%NEPROCN)
IF (LNEPROCN) THEN
  !$ACC EXIT DATA DETACH (SELF%NEPROCN)
  !$ACC EXIT DATA DELETE (SELF%NEPROCN)
ENDIF

LMYENS = ASSOCIATED (SELF%MYENS)
IF (LMYENS) THEN
  !$ACC EXIT DATA DETACH (SELF%MYENS)
  !$ACC EXIT DATA DELETE (SELF%MYENS)
ENDIF

LNUEMPP = ASSOCIATED (SELF%NUEMPP)
IF (LNUEMPP) THEN
  !$ACC EXIT DATA DETACH (SELF%NUEMPP)
  !$ACC EXIT DATA DELETE (SELF%NUEMPP)
ENDIF

LNEALLNS = ASSOCIATED (SELF%NEALLNS)
IF (LNEALLNS) THEN
  !$ACC EXIT DATA DETACH (SELF%NEALLNS)
  !$ACC EXIT DATA DELETE (SELF%NEALLNS)
ENDIF

LNEPTRNS = ASSOCIATED (SELF%NEPTRNS)
IF (LNEPTRNS) THEN
  !$ACC EXIT DATA DETACH (SELF%NEPTRNS)
  !$ACC EXIT DATA DELETE (SELF%NEPTRNS)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

FUNCTION ACDC_SIZE_TEMMP (SELF, CDPATH, LDPRINT) RESULT (KSIZE)

IMPLICIT NONE
TYPE (TEMMP),     INTENT (IN), TARGET :: SELF
CHARACTER(LEN=*), INTENT (IN), OPTIONAL :: CDPATH
LOGICAL,          INTENT (IN), OPTIONAL :: LDPRINT
INTEGER*8 :: KSIZE
INTEGER*8 :: ISIZE, JSIZE
LOGICAL :: LLPRINT
CHARACTER(LEN=128) :: CLPATH
LOGICAL :: LMYENS, LNEALLNS, LNEPROCN, LNEPTRNS, LNUEMPP
LLPRINT = .FALSE.
IF (PRESENT (LDPRINT)) THEN
  LLPRINT = LDPRINT
ENDIF
CLPATH=''
IF (PRESENT (CDPATH)) THEN
  CLPATH = CDPATH
ENDIF
KSIZE = 0
LNEPROCN = ASSOCIATED (SELF%NEPROCN)
IF (LNEPROCN) THEN
  ISIZE = KIND (SELF%NEPROCN) * SIZE (SELF%NEPROCN)
  IF (LLPRINT) THEN
    WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
    WRITE (*, *) TRIM (CLPATH)//'%NEPROCN'
  ENDIF
  KSIZE = KSIZE + ISIZE
ENDIF
ISIZE = KIND (SELF%NUEMP)
IF (LLPRINT) THEN
  WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
  WRITE (*, *) TRIM (CLPATH)//'%NUEMP'
ENDIF
KSIZE = KSIZE + ISIZE
LMYENS = ASSOCIATED (SELF%MYENS)
IF (LMYENS) THEN
  ISIZE = KIND (SELF%MYENS) * SIZE (SELF%MYENS)
  IF (LLPRINT) THEN
    WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
    WRITE (*, *) TRIM (CLPATH)//'%MYENS'
  ENDIF
  KSIZE = KSIZE + ISIZE
ENDIF
LNUEMPP = ASSOCIATED (SELF%NUEMPP)
IF (LNUEMPP) THEN
  ISIZE = KIND (SELF%NUEMPP) * SIZE (SELF%NUEMPP)
  IF (LLPRINT) THEN
    WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
    WRITE (*, *) TRIM (CLPATH)//'%NUEMPP'
  ENDIF
  KSIZE = KSIZE + ISIZE
ENDIF
LNEALLNS = ASSOCIATED (SELF%NEALLNS)
IF (LNEALLNS) THEN
  ISIZE = KIND (SELF%NEALLNS) * SIZE (SELF%NEALLNS)
  IF (LLPRINT) THEN
    WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
    WRITE (*, *) TRIM (CLPATH)//'%NEALLNS'
  ENDIF
  KSIZE = KSIZE + ISIZE
ENDIF
LNEPTRNS = ASSOCIATED (SELF%NEPTRNS)
IF (LNEPTRNS) THEN
  ISIZE = KIND (SELF%NEPTRNS) * SIZE (SELF%NEPTRNS)
  IF (LLPRINT) THEN
    WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
    WRITE (*, *) TRIM (CLPATH)//'%NEPTRNS'
  ENDIF
  KSIZE = KSIZE + ISIZE
ENDIF
END FUNCTION

END MODULE
