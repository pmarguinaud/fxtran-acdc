SUBMODULE (YOMPADDH) YOMPADDH_TPADDH_WIPE_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_WIPE_TPADDH (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
CLASS (TPADDH), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LNGLALIST, LNGPUMASK
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LNGLALIST = ALLOCATED (SELF%NGLALIST)
IF (LNGLALIST) THEN
  !$ACC EXIT DATA DETACH (SELF%NGLALIST)
  !$ACC EXIT DATA DELETE (SELF%NGLALIST)
ENDIF

LNGPUMASK = ALLOCATED (SELF%NGPUMASK)
IF (LNGPUMASK) THEN
  !$ACC EXIT DATA DETACH (SELF%NGPUMASK)
  !$ACC EXIT DATA DELETE (SELF%NGPUMASK)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

END SUBMODULE
