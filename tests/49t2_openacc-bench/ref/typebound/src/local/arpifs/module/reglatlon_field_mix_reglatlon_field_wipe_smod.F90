SUBMODULE (REGLATLON_FIELD_MIX) REGLATLON_FIELD_MIX_REGLATLON_FIELD_WIPE_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_WIPE_REGLATLON_FIELD (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
CLASS (REGLATLON_FIELD), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LPFLD, LPLAT, LPLON, LPSIN
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF

LPFLD = ASSOCIATED (SELF%PFLD)
IF (LPFLD) THEN
  !$ACC EXIT DATA DETACH (SELF%PFLD)
  !$ACC EXIT DATA DELETE (SELF%PFLD)
ENDIF

LPLAT = ASSOCIATED (SELF%PLAT)
IF (LPLAT) THEN
  !$ACC EXIT DATA DETACH (SELF%PLAT)
  !$ACC EXIT DATA DELETE (SELF%PLAT)
ENDIF

LPSIN = ASSOCIATED (SELF%PSIN)
IF (LPSIN) THEN
  !$ACC EXIT DATA DETACH (SELF%PSIN)
  !$ACC EXIT DATA DELETE (SELF%PSIN)
ENDIF

LPLON = ASSOCIATED (SELF%PLON)
IF (LPLON) THEN
  !$ACC EXIT DATA DETACH (SELF%PLON)
  !$ACC EXIT DATA DELETE (SELF%PLON)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

END SUBMODULE
