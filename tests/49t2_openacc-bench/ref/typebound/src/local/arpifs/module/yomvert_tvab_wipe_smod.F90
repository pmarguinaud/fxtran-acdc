SUBMODULE (YOMVERT) YOMVERT_TVAB_WIPE_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_WIPE_TVAB (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
CLASS (TVAB), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LVAF, LVAH, LVALH, LVBF, LVBH, LVC, LVDELA, LVDELB
LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LVALH = ALLOCATED (SELF%VALH)
IF (LVALH) THEN
  !$ACC EXIT DATA DETACH (SELF%VALH)
  !$ACC EXIT DATA DELETE (SELF%VALH)
ENDIF

LVBH = ALLOCATED (SELF%VBH)
IF (LVBH) THEN
  !$ACC EXIT DATA DETACH (SELF%VBH)
  !$ACC EXIT DATA DELETE (SELF%VBH)
ENDIF

LVAH = ALLOCATED (SELF%VAH)
IF (LVAH) THEN
  !$ACC EXIT DATA DETACH (SELF%VAH)
  !$ACC EXIT DATA DELETE (SELF%VAH)
ENDIF

LVC = ALLOCATED (SELF%VC)
IF (LVC) THEN
  !$ACC EXIT DATA DETACH (SELF%VC)
  !$ACC EXIT DATA DELETE (SELF%VC)
ENDIF

LVAF = ALLOCATED (SELF%VAF)
IF (LVAF) THEN
  !$ACC EXIT DATA DETACH (SELF%VAF)
  !$ACC EXIT DATA DELETE (SELF%VAF)
ENDIF

LVBF = ALLOCATED (SELF%VBF)
IF (LVBF) THEN
  !$ACC EXIT DATA DETACH (SELF%VBF)
  !$ACC EXIT DATA DELETE (SELF%VBF)
ENDIF

LVDELA = ALLOCATED (SELF%VDELA)
IF (LVDELA) THEN
  !$ACC EXIT DATA DETACH (SELF%VDELA)
  !$ACC EXIT DATA DELETE (SELF%VDELA)
ENDIF

LVDELB = ALLOCATED (SELF%VDELB)
IF (LVDELB) THEN
  !$ACC EXIT DATA DETACH (SELF%VDELB)
  !$ACC EXIT DATA DELETE (SELF%VDELB)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

END SUBMODULE
