MODULE UTIL_TYPE_AERO_WVL_DIAG_MOD

USE YOE_AERODIAG, ONLY : TYPE_AERO_WVL_DIAG

INTERFACE SAVE
MODULE PROCEDURE SAVE_TYPE_AERO_WVL_DIAG
END INTERFACE

INTERFACE LOAD
MODULE PROCEDURE LOAD_TYPE_AERO_WVL_DIAG
END INTERFACE

INTERFACE COPY
MODULE PROCEDURE COPY_TYPE_AERO_WVL_DIAG
END INTERFACE

INTERFACE HOST
MODULE PROCEDURE HOST_TYPE_AERO_WVL_DIAG
END INTERFACE

INTERFACE LEGACY
MODULE PROCEDURE LEGACY_TYPE_AERO_WVL_DIAG
END INTERFACE
     
INTERFACE CRC64
MODULE PROCEDURE CRC64_TYPE_AERO_WVL_DIAG
END INTERFACE

INTERFACE WIPE
MODULE PROCEDURE WIPE_TYPE_AERO_WVL_DIAG
END INTERFACE

INTERFACE SIZE
MODULE PROCEDURE SIZE_TYPE_AERO_WVL_DIAG
END INTERFACE


CONTAINS

SUBROUTINE SAVE_TYPE_AERO_WVL_DIAG (SELF, KLUN)

IMPLICIT NONE
TYPE (TYPE_AERO_WVL_DIAG), INTENT (IN), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN

WRITE (KLUN) SELF%IWVL
WRITE (KLUN) SELF%IGRIBDIAG
END SUBROUTINE

SUBROUTINE LOAD_TYPE_AERO_WVL_DIAG (SELF, KLUN)

IMPLICIT NONE
TYPE (TYPE_AERO_WVL_DIAG), INTENT (OUT), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN

READ (KLUN) SELF%IWVL
READ (KLUN) SELF%IGRIBDIAG
END SUBROUTINE


SUBROUTINE COPY_TYPE_AERO_WVL_DIAG (SELF, LDCREATED, LDFIELDAPI)

IMPLICIT NONE
TYPE (TYPE_AERO_WVL_DIAG), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDCREATED, LDFIELDAPI
LOGICAL :: LLCREATED
LOGICAL :: LLFIELDAPI

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LLCREATED = .FALSE.
IF (PRESENT (LDCREATED)) THEN
  LLCREATED = LDCREATED
ENDIF
IF (.NOT. LLCREATED) THEN
  !$ACC ENTER DATA CREATE (SELF)
  !$ACC UPDATE DEVICE (SELF)
ENDIF

END SUBROUTINE

SUBROUTINE HOST_TYPE_AERO_WVL_DIAG (SELF)

IMPLICIT NONE
TYPE (TYPE_AERO_WVL_DIAG), TARGET :: SELF


END SUBROUTINE

SUBROUTINE LEGACY_TYPE_AERO_WVL_DIAG (SELF, KADDRL, KADDRU, KDIR)

IMPLICIT NONE
TYPE (TYPE_AERO_WVL_DIAG), TARGET :: SELF
INTEGER*8, INTENT (IN) :: KADDRL
INTEGER*8, INTENT (IN) :: KADDRU
INTEGER, INTENT (IN) :: KDIR


END SUBROUTINE     

SUBROUTINE CRC64_TYPE_AERO_WVL_DIAG (SELF, KLUN, CDPATH)
USE CRC64_INTRINSIC, ONLY : FCRC64 => CRC64

IMPLICIT NONE
TYPE (TYPE_AERO_WVL_DIAG), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN
CHARACTER(LEN=*), INTENT (IN) :: CDPATH
CHARACTER(LEN=128) :: CLIND
WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%IWVL), CDPATH//'%IWVL'

WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%IGRIBDIAG), CDPATH//'%IGRIBDIAG'

END SUBROUTINE

SUBROUTINE WIPE_TYPE_AERO_WVL_DIAG (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
TYPE (TYPE_AERO_WVL_DIAG), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

FUNCTION SIZE_TYPE_AERO_WVL_DIAG (SELF, CDPATH, LDPRINT) RESULT (KSIZE)

IMPLICIT NONE
TYPE (TYPE_AERO_WVL_DIAG),     INTENT (IN), TARGET :: SELF
CHARACTER(LEN=*), INTENT (IN), OPTIONAL :: CDPATH
LOGICAL,          INTENT (IN), OPTIONAL :: LDPRINT
INTEGER*8 :: KSIZE
INTEGER*8 :: ISIZE, JSIZE
LOGICAL :: LLPRINT
CHARACTER(LEN=128) :: CLPATH

LLPRINT = .FALSE.
IF (PRESENT (LDPRINT)) THEN
  LLPRINT = LDPRINT
ENDIF
CLPATH=''
IF (PRESENT (CDPATH)) THEN
  CLPATH = CDPATH
ENDIF
KSIZE = 0
ISIZE = KIND (SELF%IWVL)
IF (LLPRINT) THEN
  WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
  WRITE (*, *) TRIM (CLPATH)//'%IWVL'
ENDIF
KSIZE = KSIZE + ISIZE
ISIZE = KIND (SELF%IGRIBDIAG) * SIZE (SELF%IGRIBDIAG)
IF (LLPRINT) THEN
  WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
  WRITE (*, *) TRIM (CLPATH)//'%IGRIBDIAG'
ENDIF
KSIZE = KSIZE + ISIZE
END FUNCTION

END MODULE
