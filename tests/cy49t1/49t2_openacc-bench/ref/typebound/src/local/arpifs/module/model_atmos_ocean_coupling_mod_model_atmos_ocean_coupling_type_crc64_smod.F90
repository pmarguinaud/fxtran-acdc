SUBMODULE (MODEL_ATMOS_OCEAN_COUPLING_MOD) MODEL_ATMOS_OCEAN_COUPLING_MOD_MODEL_ATMOS_OCEAN_COUPLING_TYPE_CRC64_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE CRC64_MODEL_ATMOS_OCEAN_COUPLING_TYPE (SELF, KLUN, CDPATH)
USE CRC64_INTRINSIC, ONLY : FCRC64 => CRC64

IMPLICIT NONE
CLASS (MODEL_ATMOS_OCEAN_COUPLING_TYPE), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN
CHARACTER(LEN=*), INTENT (IN) :: CDPATH
CHARACTER(LEN=128) :: CLIND
CLIND = ''
CALL SELF%YRMCC%CRC64 (KLUN, CDPATH//'%YRMCC'//TRIM(CLIND))

CLIND = ''
CALL SELF%YRCOM%CRC64 (KLUN, CDPATH//'%YRCOM'//TRIM(CLIND))

CLIND = ''
CALL SELF%YRCOU%CRC64 (KLUN, CDPATH//'%YRCOU'//TRIM(CLIND))

END SUBROUTINE

END SUBMODULE
