MODULE UTIL_TCOMPO_EMIS_AUX_MOD

USE YOMCOMPO, ONLY : TCOMPO_EMIS_AUX

INTERFACE SAVE
MODULE PROCEDURE SAVE_TCOMPO_EMIS_AUX
END INTERFACE

INTERFACE LOAD
MODULE PROCEDURE LOAD_TCOMPO_EMIS_AUX
END INTERFACE

INTERFACE COPY
MODULE PROCEDURE COPY_TCOMPO_EMIS_AUX
END INTERFACE

INTERFACE HOST
MODULE PROCEDURE HOST_TCOMPO_EMIS_AUX
END INTERFACE

     
INTERFACE CRC64
MODULE PROCEDURE CRC64_TCOMPO_EMIS_AUX
END INTERFACE

INTERFACE WIPE
MODULE PROCEDURE WIPE_TCOMPO_EMIS_AUX
END INTERFACE

INTERFACE SIZE
MODULE PROCEDURE SIZE_TCOMPO_EMIS_AUX
END INTERFACE


CONTAINS

SUBROUTINE SAVE_TCOMPO_EMIS_AUX (SELF, KLUN)

IMPLICIT NONE
TYPE (TCOMPO_EMIS_AUX), INTENT (IN), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN

WRITE (KLUN) SELF%PARAMID
WRITE (KLUN) SELF%TEMPORALITY
WRITE (KLUN) SELF%CNAME
END SUBROUTINE

SUBROUTINE LOAD_TCOMPO_EMIS_AUX (SELF, KLUN)

IMPLICIT NONE
TYPE (TCOMPO_EMIS_AUX), INTENT (OUT), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN

READ (KLUN) SELF%PARAMID
READ (KLUN) SELF%TEMPORALITY
READ (KLUN) SELF%CNAME
END SUBROUTINE


SUBROUTINE COPY_TCOMPO_EMIS_AUX (SELF, LDCREATED, LDFIELDAPI)

IMPLICIT NONE
TYPE (TCOMPO_EMIS_AUX), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDCREATED, LDFIELDAPI
LOGICAL :: LLCREATED
LOGICAL :: LLFIELDAPI

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LLCREATED = .FALSE.
IF (PRESENT (LDCREATED)) THEN
  LLCREATED = LDCREATED
ENDIF
IF (.NOT. LLCREATED) THEN
  !$ACC ENTER DATA CREATE (SELF)
  !$ACC UPDATE DEVICE (SELF)
ENDIF

END SUBROUTINE

SUBROUTINE HOST_TCOMPO_EMIS_AUX (SELF)

IMPLICIT NONE
TYPE (TCOMPO_EMIS_AUX), TARGET :: SELF


END SUBROUTINE

     

SUBROUTINE CRC64_TCOMPO_EMIS_AUX (SELF, KLUN, CDPATH)
USE CRC64_INTRINSIC, ONLY : FCRC64 => CRC64

IMPLICIT NONE
TYPE (TCOMPO_EMIS_AUX), TARGET :: SELF
INTEGER, INTENT (IN) :: KLUN
CHARACTER(LEN=*), INTENT (IN) :: CDPATH
CHARACTER(LEN=128) :: CLIND
WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%PARAMID), CDPATH//'%PARAMID'

WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%TEMPORALITY), CDPATH//'%TEMPORALITY'

WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (SELF%CNAME), CDPATH//'%CNAME'

END SUBROUTINE

SUBROUTINE WIPE_TCOMPO_EMIS_AUX (SELF, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
TYPE (TCOMPO_EMIS_AUX), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE

FUNCTION SIZE_TCOMPO_EMIS_AUX (SELF, CDPATH, LDPRINT) RESULT (KSIZE)

IMPLICIT NONE
TYPE (TCOMPO_EMIS_AUX),     INTENT (IN), TARGET :: SELF
CHARACTER(LEN=*), INTENT (IN), OPTIONAL :: CDPATH
LOGICAL,          INTENT (IN), OPTIONAL :: LDPRINT
INTEGER*8 :: KSIZE
INTEGER*8 :: ISIZE, JSIZE
LOGICAL :: LLPRINT
CHARACTER(LEN=128) :: CLPATH

LLPRINT = .FALSE.
IF (PRESENT (LDPRINT)) THEN
  LLPRINT = LDPRINT
ENDIF
CLPATH=''
IF (PRESENT (CDPATH)) THEN
  CLPATH = CDPATH
ENDIF
KSIZE = 0
ISIZE = KIND (SELF%PARAMID)
IF (LLPRINT) THEN
  WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
  WRITE (*, *) TRIM (CLPATH)//'%PARAMID'
ENDIF
KSIZE = KSIZE + ISIZE
ISIZE = KIND (SELF%TEMPORALITY) * LEN (SELF%TEMPORALITY)
IF (LLPRINT) THEN
  WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
  WRITE (*, *) TRIM (CLPATH)//'%TEMPORALITY'
ENDIF
KSIZE = KSIZE + ISIZE
ISIZE = KIND (SELF%CNAME) * LEN (SELF%CNAME)
IF (LLPRINT) THEN
  WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
  WRITE (*, *) TRIM (CLPATH)//'%CNAME'
ENDIF
KSIZE = KSIZE + ISIZE
END FUNCTION

END MODULE
