SUBMODULE (YOM_YGFL) YOM_YGFL_TYPE_GFLD_LEGACY_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE ACDC_LEGACY_TYPE_GFLD (SELF, KADDRL, KADDRU, KDIR)

IMPLICIT NONE
CLASS (TYPE_GFLD), TARGET :: SELF
INTEGER*8, INTENT (IN) :: KADDRL
INTEGER*8, INTENT (IN) :: KADDRU
INTEGER, INTENT (IN) :: KDIR
INTEGER :: J1, J2
LOGICAL :: LYA, LYAERAOT, LYAERLISI, LYAERO, LYAEROCLIM, LYAEROUT, LYAMF, LYCHEM, LYCPF, LYCVGQ
LOGICAL :: LYCVV, LYDAL, LYDOM, LYEDRP, LYEFB1, LYEFB2, LYEFB3, LYEMIS3D, LYERA40, LYEXT
LOGICAL :: LYEZDIAG, LYFORC, LYFQTUR, LYFSD, LYFSTUR, LYG, LYGHG, LYH, LYI, LYICONV
LOGICAL :: LYIMF, LYIRAD, LYL, LYLASTCOMP, LYLCONV, LYLIMA, LYLMF, LYLRAD, LYLRCH4, LYMXL
LOGICAL :: LYNOGW, LYO3, LYPHYCTY, LYPHYS, LYQ, LYQVA, LYR, LYRCONV, LYRKTH, LYRKTQC
LOGICAL :: LYRKTQV, LYRSPEC, LYS, LYSCONV, LYSDSAT, LYSHTUR, LYSLDIA, LYSPF, LYSRC, LYTKE
LOGICAL :: LYTTE, LYUAL, LYUEN, LYUNEBH, LYUOM, LYUVP

DO J1 = LBOUND (SELF%YCOMP, 1), UBOUND (SELF%YCOMP, 1)
  CALL SELF%YCOMP (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

LYLASTCOMP = ASSOCIATED (SELF%YLASTCOMP)
IF (LYLASTCOMP) THEN
  CALL SELF%YLASTCOMP%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYQ = ASSOCIATED (SELF%YQ)
IF (LYQ) THEN
  CALL SELF%YQ%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYI = ASSOCIATED (SELF%YI)
IF (LYI) THEN
  CALL SELF%YI%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYL = ASSOCIATED (SELF%YL)
IF (LYL) THEN
  CALL SELF%YL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYLCONV = ASSOCIATED (SELF%YLCONV)
IF (LYLCONV) THEN
  CALL SELF%YLCONV%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYICONV = ASSOCIATED (SELF%YICONV)
IF (LYICONV) THEN
  CALL SELF%YICONV%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYRCONV = ASSOCIATED (SELF%YRCONV)
IF (LYRCONV) THEN
  CALL SELF%YRCONV%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYSCONV = ASSOCIATED (SELF%YSCONV)
IF (LYSCONV) THEN
  CALL SELF%YSCONV%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYIRAD = ASSOCIATED (SELF%YIRAD)
IF (LYIRAD) THEN
  CALL SELF%YIRAD%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYLRAD = ASSOCIATED (SELF%YLRAD)
IF (LYLRAD) THEN
  CALL SELF%YLRAD%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYS = ASSOCIATED (SELF%YS)
IF (LYS) THEN
  CALL SELF%YS%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYR = ASSOCIATED (SELF%YR)
IF (LYR) THEN
  CALL SELF%YR%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYG = ASSOCIATED (SELF%YG)
IF (LYG) THEN
  CALL SELF%YG%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYH = ASSOCIATED (SELF%YH)
IF (LYH) THEN
  CALL SELF%YH%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYTKE = ASSOCIATED (SELF%YTKE)
IF (LYTKE) THEN
  CALL SELF%YTKE%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYTTE = ASSOCIATED (SELF%YTTE)
IF (LYTTE) THEN
  CALL SELF%YTTE%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYEFB1 = ASSOCIATED (SELF%YEFB1)
IF (LYEFB1) THEN
  CALL SELF%YEFB1%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYEFB2 = ASSOCIATED (SELF%YEFB2)
IF (LYEFB2) THEN
  CALL SELF%YEFB2%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYEFB3 = ASSOCIATED (SELF%YEFB3)
IF (LYEFB3) THEN
  CALL SELF%YEFB3%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYA = ASSOCIATED (SELF%YA)
IF (LYA) THEN
  CALL SELF%YA%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYO3 = ASSOCIATED (SELF%YO3)
IF (LYO3) THEN
  CALL SELF%YO3%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYSRC = ASSOCIATED (SELF%YSRC)
IF (LYSRC) THEN
  CALL SELF%YSRC%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYLMF = ASSOCIATED (SELF%YLMF)
IF (LYLMF) THEN
  CALL SELF%YLMF%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYIMF = ASSOCIATED (SELF%YIMF)
IF (LYIMF) THEN
  CALL SELF%YIMF%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYAMF = ASSOCIATED (SELF%YAMF)
IF (LYAMF) THEN
  CALL SELF%YAMF%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYMXL = ASSOCIATED (SELF%YMXL)
IF (LYMXL) THEN
  CALL SELF%YMXL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYSHTUR = ASSOCIATED (SELF%YSHTUR)
IF (LYSHTUR) THEN
  CALL SELF%YSHTUR%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYFQTUR = ASSOCIATED (SELF%YFQTUR)
IF (LYFQTUR) THEN
  CALL SELF%YFQTUR%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYFSTUR = ASSOCIATED (SELF%YFSTUR)
IF (LYFSTUR) THEN
  CALL SELF%YFSTUR%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYCPF = ASSOCIATED (SELF%YCPF)
IF (LYCPF) THEN
  CALL SELF%YCPF%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYSPF = ASSOCIATED (SELF%YSPF)
IF (LYSPF) THEN
  CALL SELF%YSPF%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYCVGQ = ASSOCIATED (SELF%YCVGQ)
IF (LYCVGQ) THEN
  CALL SELF%YCVGQ%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYQVA = ASSOCIATED (SELF%YQVA)
IF (LYQVA) THEN
  CALL SELF%YQVA%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYGHG = ASSOCIATED (SELF%YGHG)
IF (LYGHG) THEN
  DO J1 = LBOUND (SELF%YGHG, 1), UBOUND (SELF%YGHG, 1)
    CALL SELF%YGHG (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYCHEM = ASSOCIATED (SELF%YCHEM)
IF (LYCHEM) THEN
  DO J1 = LBOUND (SELF%YCHEM, 1), UBOUND (SELF%YCHEM, 1)
    CALL SELF%YCHEM (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYAERO = ASSOCIATED (SELF%YAERO)
IF (LYAERO) THEN
  DO J1 = LBOUND (SELF%YAERO, 1), UBOUND (SELF%YAERO, 1)
    CALL SELF%YAERO (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYLRCH4 = ASSOCIATED (SELF%YLRCH4)
IF (LYLRCH4) THEN
  CALL SELF%YLRCH4%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYEMIS3D = ASSOCIATED (SELF%YEMIS3D)
IF (LYEMIS3D) THEN
  DO J1 = LBOUND (SELF%YEMIS3D, 1), UBOUND (SELF%YEMIS3D, 1)
    CALL SELF%YEMIS3D (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYFORC = ASSOCIATED (SELF%YFORC)
IF (LYFORC) THEN
  DO J1 = LBOUND (SELF%YFORC, 1), UBOUND (SELF%YFORC, 1)
    CALL SELF%YFORC (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYEZDIAG = ASSOCIATED (SELF%YEZDIAG)
IF (LYEZDIAG) THEN
  DO J1 = LBOUND (SELF%YEZDIAG, 1), UBOUND (SELF%YEZDIAG, 1)
    CALL SELF%YEZDIAG (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYERA40 = ASSOCIATED (SELF%YERA40)
IF (LYERA40) THEN
  DO J1 = LBOUND (SELF%YERA40, 1), UBOUND (SELF%YERA40, 1)
    CALL SELF%YERA40 (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYNOGW = ASSOCIATED (SELF%YNOGW)
IF (LYNOGW) THEN
  DO J1 = LBOUND (SELF%YNOGW, 1), UBOUND (SELF%YNOGW, 1)
    CALL SELF%YNOGW (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYEDRP = ASSOCIATED (SELF%YEDRP)
IF (LYEDRP) THEN
  DO J1 = LBOUND (SELF%YEDRP, 1), UBOUND (SELF%YEDRP, 1)
    CALL SELF%YEDRP (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYSLDIA = ASSOCIATED (SELF%YSLDIA)
IF (LYSLDIA) THEN
  DO J1 = LBOUND (SELF%YSLDIA, 1), UBOUND (SELF%YSLDIA, 1)
    CALL SELF%YSLDIA (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYAERAOT = ASSOCIATED (SELF%YAERAOT)
IF (LYAERAOT) THEN
  DO J1 = LBOUND (SELF%YAERAOT, 1), UBOUND (SELF%YAERAOT, 1)
    CALL SELF%YAERAOT (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYAERLISI = ASSOCIATED (SELF%YAERLISI)
IF (LYAERLISI) THEN
  DO J1 = LBOUND (SELF%YAERLISI, 1), UBOUND (SELF%YAERLISI, 1)
    CALL SELF%YAERLISI (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYAEROUT = ASSOCIATED (SELF%YAEROUT)
IF (LYAEROUT) THEN
  DO J1 = LBOUND (SELF%YAEROUT, 1), UBOUND (SELF%YAEROUT, 1)
    CALL SELF%YAEROUT (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYAEROCLIM = ASSOCIATED (SELF%YAEROCLIM)
IF (LYAEROCLIM) THEN
  DO J1 = LBOUND (SELF%YAEROCLIM, 1), UBOUND (SELF%YAEROCLIM, 1)
    CALL SELF%YAEROCLIM (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYUVP = ASSOCIATED (SELF%YUVP)
IF (LYUVP) THEN
  DO J1 = LBOUND (SELF%YUVP, 1), UBOUND (SELF%YUVP, 1)
    CALL SELF%YUVP (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYPHYS = ASSOCIATED (SELF%YPHYS)
IF (LYPHYS) THEN
  DO J1 = LBOUND (SELF%YPHYS, 1), UBOUND (SELF%YPHYS, 1)
    CALL SELF%YPHYS (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYPHYCTY = ASSOCIATED (SELF%YPHYCTY)
IF (LYPHYCTY) THEN
  CALL SELF%YPHYCTY%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYRSPEC = ASSOCIATED (SELF%YRSPEC)
IF (LYRSPEC) THEN
  CALL SELF%YRSPEC%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYSDSAT = ASSOCIATED (SELF%YSDSAT)
IF (LYSDSAT) THEN
  CALL SELF%YSDSAT%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYCVV = ASSOCIATED (SELF%YCVV)
IF (LYCVV) THEN
  CALL SELF%YCVV%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYRKTH = ASSOCIATED (SELF%YRKTH)
IF (LYRKTH) THEN
  CALL SELF%YRKTH%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYRKTQV = ASSOCIATED (SELF%YRKTQV)
IF (LYRKTQV) THEN
  CALL SELF%YRKTQV%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYRKTQC = ASSOCIATED (SELF%YRKTQC)
IF (LYRKTQC) THEN
  CALL SELF%YRKTQC%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYUOM = ASSOCIATED (SELF%YUOM)
IF (LYUOM) THEN
  CALL SELF%YUOM%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYUAL = ASSOCIATED (SELF%YUAL)
IF (LYUAL) THEN
  CALL SELF%YUAL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYDOM = ASSOCIATED (SELF%YDOM)
IF (LYDOM) THEN
  CALL SELF%YDOM%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYDAL = ASSOCIATED (SELF%YDAL)
IF (LYDAL) THEN
  CALL SELF%YDAL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYUEN = ASSOCIATED (SELF%YUEN)
IF (LYUEN) THEN
  CALL SELF%YUEN%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYUNEBH = ASSOCIATED (SELF%YUNEBH)
IF (LYUNEBH) THEN
  CALL SELF%YUNEBH%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYLIMA = ASSOCIATED (SELF%YLIMA)
IF (LYLIMA) THEN
  DO J1 = LBOUND (SELF%YLIMA, 1), UBOUND (SELF%YLIMA, 1)
    CALL SELF%YLIMA (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

LYFSD = ASSOCIATED (SELF%YFSD)
IF (LYFSD) THEN
  CALL SELF%YFSD%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDIF

LYEXT = ASSOCIATED (SELF%YEXT)
IF (LYEXT) THEN
  DO J1 = LBOUND (SELF%YEXT, 1), UBOUND (SELF%YEXT, 1)
    CALL SELF%YEXT (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDIF

CALL SELF%YQ_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YI_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YL_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YLCONV_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YICONV_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YRCONV_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YSCONV_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YIRAD_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YLRAD_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YS_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YR_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YG_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YH_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YTKE_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YTTE_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YEFB1_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YEFB2_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YEFB3_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YA_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YO3_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YSRC_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YAMF_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YLMF_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YIMF_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YMXL_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YSHTUR_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YFQTUR_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YFSTUR_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YCPF_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YSPF_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YCVGQ_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YQVA_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

DO J1 = LBOUND (SELF%YGHG_NL, 1), UBOUND (SELF%YGHG_NL, 1)
  CALL SELF%YGHG_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

DO J1 = LBOUND (SELF%YCHEM_NL, 1), UBOUND (SELF%YCHEM_NL, 1)
  CALL SELF%YCHEM_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

DO J1 = LBOUND (SELF%YAERO_NL, 1), UBOUND (SELF%YAERO_NL, 1)
  CALL SELF%YAERO_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

DO J1 = LBOUND (SELF%YERA40_NL, 1), UBOUND (SELF%YERA40_NL, 1)
  CALL SELF%YERA40_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

DO J1 = LBOUND (SELF%YNOGW_NL, 1), UBOUND (SELF%YNOGW_NL, 1)
  CALL SELF%YNOGW_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

DO J1 = LBOUND (SELF%YEDRP_NL, 1), UBOUND (SELF%YEDRP_NL, 1)
  CALL SELF%YEDRP_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

DO J1 = LBOUND (SELF%YSLDIA_NL, 1), UBOUND (SELF%YSLDIA_NL, 1)
  CALL SELF%YSLDIA_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

CALL SELF%YLRCH4_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

DO J1 = LBOUND (SELF%YAERAOT_NL, 1), UBOUND (SELF%YAERAOT_NL, 1)
  CALL SELF%YAERAOT_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

DO J2 = LBOUND (SELF%YAERLISI_NL, 2), UBOUND (SELF%YAERLISI_NL, 2)
  DO J1 = LBOUND (SELF%YAERLISI_NL, 1), UBOUND (SELF%YAERLISI_NL, 1)
    CALL SELF%YAERLISI_NL (J1, J2)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
  ENDDO
ENDDO

DO J1 = LBOUND (SELF%YAEROUT_NL, 1), UBOUND (SELF%YAEROUT_NL, 1)
  CALL SELF%YAEROUT_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

DO J1 = LBOUND (SELF%YAEROCLIM_NL, 1), UBOUND (SELF%YAEROCLIM_NL, 1)
  CALL SELF%YAEROCLIM_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

DO J1 = LBOUND (SELF%YUVP_NL, 1), UBOUND (SELF%YUVP_NL, 1)
  CALL SELF%YUVP_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

CALL SELF%YRKTH_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YRKTQV_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YRKTQC_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YPHYCTY_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YFSD_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YRSPEC_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YSDSAT_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YCVV_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

DO J1 = LBOUND (SELF%YFORC_NL, 1), UBOUND (SELF%YFORC_NL, 1)
  CALL SELF%YFORC_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

DO J1 = LBOUND (SELF%YEZDIAG_NL, 1), UBOUND (SELF%YEZDIAG_NL, 1)
  CALL SELF%YEZDIAG_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

DO J1 = LBOUND (SELF%YEXT_NL, 1), UBOUND (SELF%YEXT_NL, 1)
  CALL SELF%YEXT_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

CALL SELF%YUOM_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YUAL_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YDOM_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YDAL_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YUEN_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

CALL SELF%YUNEBH_NL%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)

DO J1 = LBOUND (SELF%YAERO_WVL_DIAG_NL, 1), UBOUND (SELF%YAERO_WVL_DIAG_NL, 1)
  CALL SELF%YAERO_WVL_DIAG_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

DO J1 = LBOUND (SELF%YLIMA_NL, 1), UBOUND (SELF%YLIMA_NL, 1)
  CALL SELF%YLIMA_NL (J1)%ACDC_LEGACY (KADDRL, KADDRU, KDIR=KDIR)
ENDDO

END SUBROUTINE

END SUBMODULE
