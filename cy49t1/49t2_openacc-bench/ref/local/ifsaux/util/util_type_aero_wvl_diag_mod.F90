MODULE UTIL_TYPE_AERO_WVL_DIAG_MOD

USE YOE_AERODIAG, ONLY : TYPE_AERO_WVL_DIAG

INTERFACE SAVE
MODULE PROCEDURE SAVE_TYPE_AERO_WVL_DIAG
END INTERFACE

INTERFACE LOAD
MODULE PROCEDURE LOAD_TYPE_AERO_WVL_DIAG
END INTERFACE

INTERFACE COPY
MODULE PROCEDURE COPY_TYPE_AERO_WVL_DIAG
END INTERFACE

INTERFACE HOST
MODULE PROCEDURE HOST_TYPE_AERO_WVL_DIAG
END INTERFACE

INTERFACE LEGACY
MODULE PROCEDURE LEGACY_TYPE_AERO_WVL_DIAG
END INTERFACE
     
INTERFACE CRC64
MODULE PROCEDURE CRC64_TYPE_AERO_WVL_DIAG
END INTERFACE

INTERFACE WIPE
MODULE PROCEDURE WIPE_TYPE_AERO_WVL_DIAG
END INTERFACE

INTERFACE SIZE
MODULE PROCEDURE SIZE_TYPE_AERO_WVL_DIAG
END INTERFACE


CONTAINS

SUBROUTINE SAVE_TYPE_AERO_WVL_DIAG (KLUN, YD)

IMPLICIT NONE
INTEGER, INTENT (IN) :: KLUN
TYPE (TYPE_AERO_WVL_DIAG), INTENT (IN), TARGET :: YD

WRITE (KLUN) YD%IWVL
WRITE (KLUN) YD%IGRIBDIAG
END SUBROUTINE

SUBROUTINE LOAD_TYPE_AERO_WVL_DIAG (KLUN, YD)

IMPLICIT NONE
INTEGER, INTENT (IN) :: KLUN
TYPE (TYPE_AERO_WVL_DIAG), INTENT (OUT), TARGET :: YD

READ (KLUN) YD%IWVL
READ (KLUN) YD%IGRIBDIAG
END SUBROUTINE


SUBROUTINE COPY_TYPE_AERO_WVL_DIAG (YD, LDCREATED, LDFIELDAPI)

IMPLICIT NONE
TYPE (TYPE_AERO_WVL_DIAG), INTENT (IN), TARGET :: YD
LOGICAL, OPTIONAL, INTENT (IN) :: LDCREATED, LDFIELDAPI
LOGICAL :: LLCREATED
LOGICAL :: LLFIELDAPI

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LLCREATED = .FALSE.
IF (PRESENT (LDCREATED)) THEN
  LLCREATED = LDCREATED
ENDIF
IF (.NOT. LLCREATED) THEN
  !$ACC ENTER DATA CREATE (YD)
  !$ACC UPDATE DEVICE (YD)
ENDIF


END SUBROUTINE

SUBROUTINE HOST_TYPE_AERO_WVL_DIAG (YD)

IMPLICIT NONE
TYPE (TYPE_AERO_WVL_DIAG), TARGET :: YD



END SUBROUTINE

SUBROUTINE LEGACY_TYPE_AERO_WVL_DIAG (YD, KADDRL, KADDRU, KDIR)

IMPLICIT NONE
TYPE (TYPE_AERO_WVL_DIAG), TARGET :: YD
INTEGER*8, INTENT (IN) :: KADDRL
INTEGER*8, INTENT (IN) :: KADDRU
INTEGER, INTENT (IN) :: KDIR



END SUBROUTINE     

SUBROUTINE CRC64_TYPE_AERO_WVL_DIAG (YD, KLUN, CDPATH)
USE CRC64_INTRINSIC, ONLY : FCRC64 => CRC64

IMPLICIT NONE
TYPE (TYPE_AERO_WVL_DIAG), TARGET :: YD
INTEGER, INTENT (IN) :: KLUN
CHARACTER(LEN=*), INTENT (IN) :: CDPATH
CHARACTER(LEN=128) :: CLIND

WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (YD%IWVL), CDPATH//'%IWVL'

WRITE (KLUN, '(Z16.16," ",A)') FCRC64 (YD%IGRIBDIAG), CDPATH//'%IGRIBDIAG'

END SUBROUTINE

SUBROUTINE WIPE_TYPE_AERO_WVL_DIAG (YD, LDDELETED, LDFIELDAPI)

IMPLICIT NONE
TYPE (TYPE_AERO_WVL_DIAG), INTENT (IN), TARGET :: YD
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF


LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (YD)
ENDIF
END SUBROUTINE

INTEGER*8 FUNCTION SIZE_TYPE_AERO_WVL_DIAG (YD, CDPATH, LDPRINT) RESULT (KSIZE)

IMPLICIT NONE
TYPE (TYPE_AERO_WVL_DIAG),     INTENT (IN), TARGET :: YD
CHARACTER(LEN=*), INTENT (IN), OPTIONAL :: CDPATH
LOGICAL,          INTENT (IN), OPTIONAL :: LDPRINT
INTEGER*8 :: ISIZE, JSIZE
LOGICAL :: LLPRINT
CHARACTER(LEN=128) :: CLPATH

LLPRINT = .FALSE.
IF (PRESENT (LDPRINT)) THEN
  LLPRINT = LDPRINT
ENDIF
CLPATH=''
IF (PRESENT (CDPATH)) THEN
  CLPATH = CDPATH
ENDIF
KSIZE = 0
ISIZE = KIND (YD%IWVL)
IF (LLPRINT) THEN
  WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
  WRITE (*, *) TRIM (CLPATH)//'%IWVL'
ENDIF
KSIZE = KSIZE + ISIZE
ISIZE = KIND (YD%IGRIBDIAG) * SIZE (YD%IGRIBDIAG)
IF (LLPRINT) THEN
  WRITE (*, '(I10," ")', ADVANCE='NO') ISIZE
  WRITE (*, *) TRIM (CLPATH)//'%IGRIBDIAG'
ENDIF
KSIZE = KSIZE + ISIZE
END FUNCTION

END MODULE
