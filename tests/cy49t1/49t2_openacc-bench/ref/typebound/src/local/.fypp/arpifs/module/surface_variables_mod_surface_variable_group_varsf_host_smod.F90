SUBMODULE (SURFACE_VARIABLES_MOD) SURFACE_VARIABLES_MOD_SURFACE_VARIABLE_GROUP_VARSF_HOST_SMOD

IMPLICIT NONE

CONTAINS

MODULE SUBROUTINE HOST_SURFACE_VARIABLE_GROUP_VARSF (SELF)
USE FIELD_UTIL_MODULE

IMPLICIT NONE
CLASS (SURFACE_VARIABLE_GROUP_VARSF), TARGET :: SELF
INTEGER :: J1
LOGICAL :: LF_GROUP, LVCHEMDDFLX, LVCHEMDV, LVCHEMFLXO, LVCHEMWDFLX, LVEMIS2D, LVEMIS2DAUX
CALL SELF%VZ0F%HOST ()

CALL SELF%VALBF%HOST ()

CALL SELF%VEMISF%HOST ()

CALL SELF%VGETRL%HOST ()

CALL SELF%VLSM%HOST ()

CALL SELF%VVEG%HOST ()

CALL SELF%VVRLAN%HOST ()

CALL SELF%VVRLDI%HOST ()

CALL SELF%VSIG%HOST ()

CALL SELF%VALBSF%HOST ()

CALL SELF%VLAN%HOST ()

CALL SELF%VSST%HOST ()

CALL SELF%VSSS%HOST ()

CALL SELF%VLZ0H%HOST ()

CALL SELF%VCVL%HOST ()

CALL SELF%VCO2TYP%HOST ()

CALL SELF%VCVH%HOST ()

CALL SELF%VCUR%HOST ()

CALL SELF%VTVL%HOST ()

CALL SELF%VTVH%HOST ()

CALL SELF%VLAIL%HOST ()

CALL SELF%VLAIH%HOST ()

CALL SELF%VFWET%HOST ()

CALL SELF%VSOTY%HOST ()

CALL SELF%VCLK%HOST ()

CALL SELF%VDL%HOST ()

CALL SELF%VCI%HOST ()

CALL SELF%VUCUR%HOST ()

CALL SELF%VVCUR%HOST ()

CALL SELF%VZ0RLF%HOST ()

CALL SELF%VCGPP%HOST ()

CALL SELF%VCREC%HOST ()

CALL SELF%VSDFOR%HOST ()

CALL SELF%VALUVP%HOST ()

CALL SELF%VALUVD%HOST ()

CALL SELF%VALNIP%HOST ()

CALL SELF%VALNID%HOST ()

CALL SELF%VFP1%HOST ()

CALL SELF%VSO2DD%HOST ()

CALL SELF%VDMSO%HOST ()

CALL SELF%VURBF%HOST ()

CALL SELF%VFCA1%HOST ()

CALL SELF%VFCA2%HOST ()

CALL SELF%VAERDEP%HOST ()

CALL SELF%VAERLTS%HOST ()

CALL SELF%VAERSCC%HOST ()

CALL SELF%VDSF%HOST ()

LVCHEMFLXO = ALLOCATED (SELF%VCHEMFLXO)
IF (LVCHEMFLXO) THEN
  DO J1 = LBOUND (SELF%VCHEMFLXO, 1), UBOUND (SELF%VCHEMFLXO, 1)
    CALL SELF%VCHEMFLXO (J1)%HOST ()
  ENDDO
ENDIF

LVCHEMWDFLX = ALLOCATED (SELF%VCHEMWDFLX)
IF (LVCHEMWDFLX) THEN
  DO J1 = LBOUND (SELF%VCHEMWDFLX, 1), UBOUND (SELF%VCHEMWDFLX, 1)
    CALL SELF%VCHEMWDFLX (J1)%HOST ()
  ENDDO
ENDIF

LVCHEMDDFLX = ALLOCATED (SELF%VCHEMDDFLX)
IF (LVCHEMDDFLX) THEN
  DO J1 = LBOUND (SELF%VCHEMDDFLX, 1), UBOUND (SELF%VCHEMDDFLX, 1)
    CALL SELF%VCHEMDDFLX (J1)%HOST ()
  ENDDO
ENDIF

LVCHEMDV = ALLOCATED (SELF%VCHEMDV)
IF (LVCHEMDV) THEN
  DO J1 = LBOUND (SELF%VCHEMDV, 1), UBOUND (SELF%VCHEMDV, 1)
    CALL SELF%VCHEMDV (J1)%HOST ()
  ENDDO
ENDIF

CALL SELF%VNUDM%HOST ()

LVEMIS2D = ALLOCATED (SELF%VEMIS2D)
IF (LVEMIS2D) THEN
  DO J1 = LBOUND (SELF%VEMIS2D, 1), UBOUND (SELF%VEMIS2D, 1)
    CALL SELF%VEMIS2D (J1)%HOST ()
  ENDDO
ENDIF

LVEMIS2DAUX = ALLOCATED (SELF%VEMIS2DAUX)
IF (LVEMIS2DAUX) THEN
  DO J1 = LBOUND (SELF%VEMIS2DAUX, 1), UBOUND (SELF%VEMIS2DAUX, 1)
    CALL SELF%VEMIS2DAUX (J1)%HOST ()
  ENDDO
ENDIF

LF_GROUP = ASSOCIATED (SELF%F_GROUP)
IF (LF_GROUP) THEN
  CALL HOST_FIELD_3RB (SELF%F_GROUP)
ENDIF

END SUBROUTINE

END SUBMODULE
