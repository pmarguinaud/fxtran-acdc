MODULE UTIL_SURFACE_VIEW_GROUP_VO3ABC_MOD

USE SURFACE_VIEWS_DIAGNOSTIC_MODULE, ONLY : SURFACE_VIEW_GROUP_VO3ABC



INTERFACE COPY
MODULE PROCEDURE COPY_SURFACE_VIEW_GROUP_VO3ABC
END INTERFACE

INTERFACE HOST
MODULE PROCEDURE HOST_SURFACE_VIEW_GROUP_VO3ABC
END INTERFACE

     

INTERFACE WIPE
MODULE PROCEDURE WIPE_SURFACE_VIEW_GROUP_VO3ABC
END INTERFACE



CONTAINS





SUBROUTINE COPY_SURFACE_VIEW_GROUP_VO3ABC (SELF, LDCREATED, LDFIELDAPI)
USE FIELD_UTIL_MODULE
USE UTIL_SURFACE_VARIABLE_GROUP_VO3ABC_MOD
IMPLICIT NONE
TYPE (SURFACE_VIEW_GROUP_VO3ABC), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDCREATED, LDFIELDAPI
LOGICAL :: LLCREATED
LOGICAL :: LLFIELDAPI
LOGICAL :: LF_A, LF_B, LF_C, LF_GROUP, LVARIABLE_GROUP

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LLCREATED = .FALSE.
IF (PRESENT (LDCREATED)) THEN
  LLCREATED = LDCREATED
ENDIF
IF (.NOT. LLCREATED) THEN
  !$ACC ENTER DATA CREATE (SELF)
  !$ACC UPDATE DEVICE (SELF)
ENDIF
LVARIABLE_GROUP = ASSOCIATED (SELF%VARIABLE_GROUP)
IF (LVARIABLE_GROUP) THEN
  !$ACC ENTER DATA CREATE (SELF%VARIABLE_GROUP)
  !$ACC UPDATE DEVICE (SELF%VARIABLE_GROUP)
  CALL COPY_SURFACE_VARIABLE_GROUP_VO3ABC (SELF%VARIABLE_GROUP, LDCREATED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC ENTER DATA ATTACH (SELF%VARIABLE_GROUP)
ENDIF

LF_A = ASSOCIATED (SELF%F_A)
IF (LF_A .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_2RB (SELF%F_A)
  !$ACC ENTER DATA ATTACH (SELF%F_A)
ENDIF

LF_B = ASSOCIATED (SELF%F_B)
IF (LF_B .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_2RB (SELF%F_B)
  !$ACC ENTER DATA ATTACH (SELF%F_B)
ENDIF

LF_C = ASSOCIATED (SELF%F_C)
IF (LF_C .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_2RB (SELF%F_C)
  !$ACC ENTER DATA ATTACH (SELF%F_C)
ENDIF

LF_GROUP = ASSOCIATED (SELF%F_GROUP)
IF (LF_GROUP .AND. LLFIELDAPI) THEN
  CALL COPY_FIELD_3RB (SELF%F_GROUP)
  !$ACC ENTER DATA ATTACH (SELF%F_GROUP)
ENDIF

END SUBROUTINE

SUBROUTINE HOST_SURFACE_VIEW_GROUP_VO3ABC (SELF)
USE FIELD_UTIL_MODULE
USE UTIL_SURFACE_VARIABLE_GROUP_VO3ABC_MOD

IMPLICIT NONE
TYPE (SURFACE_VIEW_GROUP_VO3ABC), TARGET :: SELF
LOGICAL :: LF_A, LF_B, LF_C, LF_GROUP, LVARIABLE_GROUP
LVARIABLE_GROUP = ASSOCIATED (SELF%VARIABLE_GROUP)
IF (LVARIABLE_GROUP) THEN
  CALL HOST_SURFACE_VARIABLE_GROUP_VO3ABC (SELF%VARIABLE_GROUP)
ENDIF

LF_A = ASSOCIATED (SELF%F_A)
IF (LF_A) THEN
  CALL HOST_FIELD_2RB (SELF%F_A)
ENDIF

LF_B = ASSOCIATED (SELF%F_B)
IF (LF_B) THEN
  CALL HOST_FIELD_2RB (SELF%F_B)
ENDIF

LF_C = ASSOCIATED (SELF%F_C)
IF (LF_C) THEN
  CALL HOST_FIELD_2RB (SELF%F_C)
ENDIF

LF_GROUP = ASSOCIATED (SELF%F_GROUP)
IF (LF_GROUP) THEN
  CALL HOST_FIELD_3RB (SELF%F_GROUP)
ENDIF

END SUBROUTINE

     



SUBROUTINE WIPE_SURFACE_VIEW_GROUP_VO3ABC (SELF, LDDELETED, LDFIELDAPI)
USE FIELD_UTIL_MODULE
USE UTIL_SURFACE_VARIABLE_GROUP_VO3ABC_MOD
IMPLICIT NONE
TYPE (SURFACE_VIEW_GROUP_VO3ABC), INTENT (IN), TARGET :: SELF
LOGICAL, OPTIONAL, INTENT (IN) :: LDDELETED, LDFIELDAPI
LOGICAL :: LLDELETED
LOGICAL :: LLFIELDAPI
LOGICAL :: LF_A, LF_B, LF_C, LF_GROUP, LVARIABLE_GROUP

LLFIELDAPI = .FALSE.
IF (PRESENT (LDFIELDAPI)) THEN
  LLFIELDAPI = LDFIELDAPI
ENDIF
LVARIABLE_GROUP = ASSOCIATED (SELF%VARIABLE_GROUP)
IF (LVARIABLE_GROUP) THEN
  !$ACC EXIT DATA DETACH (SELF%VARIABLE_GROUP)
  CALL WIPE_SURFACE_VARIABLE_GROUP_VO3ABC (SELF%VARIABLE_GROUP, LDDELETED=.TRUE., LDFIELDAPI=LDFIELDAPI)
  !$ACC EXIT DATA DETACH (SELF%VARIABLE_GROUP)
ENDIF

LF_A = ASSOCIATED (SELF%F_A)
IF (LF_A .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_A)
ENDIF

LF_B = ASSOCIATED (SELF%F_B)
IF (LF_B .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_B)
ENDIF

LF_C = ASSOCIATED (SELF%F_C)
IF (LF_C .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_2RB (SELF%F_C)
ENDIF

LF_GROUP = ASSOCIATED (SELF%F_GROUP)
IF (LF_GROUP .AND. LLFIELDAPI) THEN
  CALL WIPE_FIELD_3RB (SELF%F_GROUP)
ENDIF

LLDELETED = .FALSE.
IF (PRESENT (LDDELETED)) THEN
  LLDELETED = LDDELETED
ENDIF
IF (.NOT. LLDELETED) THEN
  !$ACC EXIT DATA DELETE (SELF)
ENDIF
END SUBROUTINE



END MODULE
